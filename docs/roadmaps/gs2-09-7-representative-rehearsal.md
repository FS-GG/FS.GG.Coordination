# GS2-09.7 representative migration rehearsal

The [governing roadmap](https://github.com/FS-GG/.github/blob/15734f4cb71506d76814e054d0e846babf6909aa/docs/github-substrate-v2-roadmap.md#gs2-09--build-migration-archive-and-rollback-tooling) requires migration, interruption at every step, retry, rollback, rerun, and archive verification on isolated repository and Project copies. The current accepted GS2-09.1–GS2-09.6 receipts establish source contracts. They explicitly do not establish live discovery, a live manifest, an effect executor, published history, or executed rollback. GS2-09.9 supplies a callable ordinary v2 journey, not a fleet migration interpreter. These distinctions remain visible in this rehearsal's evidence.

## Execution boundary

The selected subject is a finite, declared set of isolated GitHub repositories and Project snapshots. Its provider identities, repository IDs, Project ID, capabilities, permissions, source heads, source digests, and complete pagination proofs are recorded before mutation. No production fleet subject is an effect target. The actor has only the scoped permissions required for this copy set. A missing entitlement, page, source identity, or prestate is a refusal, never an absent value.

The rehearsal consumes the exact accepted GS2-09.6 receipt and the GS2-09.9 callable handoff, then produces fresh copy-specific GS2-09.1 discovery, GS2-09.2 manifest, GS2-09.3 transforms, GS2-09.4 operation dispositions, GS2-09.5 history, and GS2-09.6 rollback inputs. Historical controlled fixtures are contract evidence; they are not substituted for the fresh provider readings. Every step is bound to one immutable manifest and exact provider prestate. Ambiguous and unsupported transforms need explicit reviewed dispositions before an effect.

The operational interpreter has a closed set of typed effects for native issue fields and types, hierarchy and blocking edges, Project state, body metadata removal, repository settings, receiver pins, scheduling, and archive publication. It uses the already qualified adapters for each surface. No arbitrary REST request or script supplied by a manifest can become an effect. Every effect persists an intent with its target, expected revision, idempotency identity, epoch generation, source and desired digests, and recovery probe before dispatch. After dispatch it rereads the authority and persists a result receipt. An unknown response is pending until authoritative readback distinguishes applied, proven absent, partial, and indeterminate. The protected journal's expected-parent CAS and generation fence remain the concurrency authority.

## Rehearsal sequence

1. Capture two complete quiescent reads and seal the copy-specific manifest, source bytes, operation population, transform decisions, archive inputs, receiver heads, settings prestate, and rollback inputs.
2. Execute the ordered migration once. At each effect, force interruptions before intent, after intent, before dispatch, after dispatch with the response lost, after readback, and before receipt. Restart from durable provider and journal state for every cut. Record the request count and authoritative target readback; a repeated effect on a known-applied target fails.
3. Re-invoke the exact completed operation. It must be a no-op with no new dispatch and the same final authority digest.
4. Verify every archive record and lookup against the frozen source bytes with the separately retained verifier. Rejected historical records retain their typed reason.
5. From the rollback-eligible verified state, execute the five GS2-09.6 restoration domains in reverse order: authority snapshot, schedules, v1 projections, receiver pins, settings. Interrupt and resume each domain from exact chained receipts; reread the restored source state and epoch. No rollback is offered after `OpenV2`.
6. Start a separately identified rehearsal round from the restored copy, bound to a fresh epoch generation and fresh complete reads. Migrate again and compare normalized end state, archive verification, operation dispositions, and receipt counts with the first round. A new round cannot reuse the first round's idempotency identity as authority.

## Acceptance and controls

Q5 binds the copy population, complete source and target reads, exact predecessor receipts, manifest, every transform and operation disposition, native relations and settings readback, archive records and verifier, and the two migration rounds. Q6 binds every interruption cut, persisted-before-effect intent, fresh-process recovery, lost-response reconciliation, settled replay, rollback receipts, and no duplicate effect. Independently authored black-box cases inject a missing terminal page, one newly added source subject, an unexpected Project item, an ambiguous transform without disposition, a changed receiver head, a wrong field type, a missing native edge, an altered manifest, a stale journal generation, an unavailable observer, a partial settings application, a lost response, and an attempted post-`OpenV2` rollback. Each control must show the precise refusal or pending state and zero unauthorized effects.

The retained report records source and final provider IDs/revisions, request and journal identities, exact command and artifact hashes, interruption point, effect count, readback, duration, API budget, refusal and unknown outcomes, cleanup, and limitations. A successful local model or in-memory copy test alone cannot satisfy this unit. The owning PR and protected acceptance receipt may mark GS2-09.7 complete only after the exact isolated provider run and independent controls pass. GS2-09.8 separately proves no omission over the exact manifest and changed-population controls.

## Implementation status

This document defines the execution touch-set and evidence shape. The repository has the source qualification contracts for GS2-09.1–GS2-09.6 and a controlled `MigrationStepExecution` boundary. It has no GitHub migration provider, multi-step driver, or copy-specific provider capture. GS2-09.7 remains unregistered and unaccepted. Future Q5/Q6 commands must require the isolated run rather than accepting controlled fixtures as provider evidence.

## Canonical protocol correspondence and implementation steps

The authored [literate Quint protocol](../../src/FS.GG.Coordination.Protocol/Protocol.md) is the behavioral constraint. `mutationIntentShapeIsValid`, `mutationResultMayFollow`, and the `MutationResultsAreBound` and `UncertainMutationOutcomesStayUnknown` invariants require a closed mutation kind, exact expected revision, stable operation and idempotency identities, and uncertainty that cannot become absence. The GS2-03.10 journal amendment requires expected-parent CAS, a fresh generation fence immediately before dispatch, durable `InFlight` intent, and recovery-only treatment of a restored in-flight effect. `DurablePlansAreOrderedAndResumable` and `DurablePlanCompensationIsBoundaryBound` require ordered receipts and explicit compensation boundaries. Existing `OrdinaryDelivery.advance` covers one source-delivery operation; GS2-09.1–GS2-09.6 provide pure migration facts and plans, with no migration execution function.

The implementation sequence, checked against the unchanged protocol, is:

1. Add a closed migration-step execution boundary in `FS.GG.Coordination.GitHub`. It binds one manifest, source/desired state, mutation kind, expected revision, epoch and journal generation, persists intent before dispatch, and classifies recovery from fresh authority reads. Controlled tests exercise interruption and unknown outcomes. Verify the mutation and durable-plan Quint roots after this step.
2. Compose provider readers and interpreters for the declared GitHub surfaces, using the existing adapters. Add an isolated-copy CLI path, protected journal integration, provider response controls, and an exact readback artifact. Verify the same Quint roots and the independent adapter tests after each surface is connected.
3. Register GS2-09.7 with its exact Q5/Q6 gate command identities only when the commands require a real isolated provider report. Run migration, every interruption cut, rollback, rerun, archive verification, and independent controls, then bind the protected acceptance receipt. A green controlled test alone cannot close the unit.

### Project observation gap plan

The canonical `authorityObservationEvidenceIsQualified`, `observationContributesKnowledge`, and `UnknownLifecycleFactsFailClosed` predicates require complete, non-contradictory provider evidence before it can contribute positive knowledge. The current read-only provider captures issue population, issue types, and Project item membership, but not Project field declarations or per-item field values. It therefore cannot satisfy the Project authority in fresh GS2-09.1 discovery.

1. Extend `MigrationGitHubRead.readProjectFields` to bind exact Project ID/number, field kinds, option IDs, total count, terminal cursor and duplicate-ID checks. A GraphQL error, unsupported field kind, missing page, changing total or schema identity refuses. Verify with independent controlled responses, a live read-only diagnostic, and the unchanged Quint observation/unknown invariants.
2. Extend `MigrationGitHubRead.readProjectItems` with every item's field-value connection. Require a complete nested page chain or refuse the entire Project read; bind each value to its declared field ID and typed value. Verify missing nested continuation, partial data, field drift, two-pass readback, and the same Quint invariants.
3. Only after both reads and every remaining authority surface exist, compose the exact nine-authority discovery and copy-specific manifest. A provider query that returns only the current partial slice cannot be called GS2-09.1 discovery or GS2-09.7 acceptance.

Rollback criterion: if canonical Quint observation or unknown-outcome properties fail, stop and revise the implementation; do not weaken the authored protocol.

Status after Project observation steps 1–2: source and independent controlled tests are green. Field declarations and typed item values are read through terminal pagination; raw provider JSON and SHA-256 remain paired. `reconcileProject` requires exact item identities and revisions, declared field IDs, matching counts and raw-byte digests across membership, fields and values. The live read-only diagnostic of the accessible Project matched two complete passes, but is not an isolated copy or a migration rehearsal. Nested connections beyond the first 100 entries and `ProjectV2ItemIssueFieldValue` are explicit refusals, not silently incomplete observations. The pinned canonical Quint Q1/Q2 qualification passed against the unchanged authored source and compiled contract (ignored receipt at `artifacts/canonical-quint/qualification.json`); this proves the canonical model, not GitHub provider correspondence. Next is the remaining authority capture and isolated-copy execution, not acceptance of GS2-09.7.

Current status: research and step 1 complete as source work. `MigrationStepExecution.sealStep` binds a closed effect family, manifest, expected and desired target digests, epoch, journal parent/generation, and explicit admission, operation, optional claim, seal, and registry-head authority. `advance` persists intent, promotes it to in-flight before dispatch, refreshes the bound epoch, authority fence, target and journal immediately before the external effect, settles from effect and target readback, and refuses blind replay of a restored in-flight operation. Its journal stages require their exact chained generations, and an observed effect cannot settle directly from `IntentPersisted` without an `InFlight` grant. Controlled tests cover intent, dispatch, lost response, settled replay, unknown/partial outcomes, stale epoch, changed target, altered plan, forged stage generation, external application before grant, and authority drift before and after journal promotion. The full pinned canonical Quint qualification passed against the unchanged authored source (receipt under ignored `artifacts/canonical-quint/`). No spec change is proposed. A failed Quint invariant stops the implementation for diagnosis; it is never repaired by weakening the authored protocol.

Step 2 has a read-only provider slice: `MigrationGitHubRead` pins repository identity, walks the complete REST issue pagination while separately counting pull requests, and walks GraphQL issue-type, Project item, Project field and per-item value pagination. It rejects malformed items, duplicate IDs, escaped continuations, skipped issue pages, absent pages, changed totals, unknown typed value shapes, nested truncation and any GraphQL `errors` field even when `data` is present. `HttpMigrationGitHubReadTransport` refuses REST non-GET and GraphQL mutation-shaped requests before network dispatch. The two-pass `eng/probe-migration-github-read.fsx` diagnostic ran against a declared private substrate sandbox: one REST page contained 2 issues and 18 pull requests, one GraphQL page contained 4 issue types, and the two raw-payload-bound passes matched at SHA-256 `aeace88f7e991bd6a9b765c105ca61f89f1edeb5f562c643e476c13d2740aa77`. A separate read-only diagnostic of the public Coordination repository exercised GitHub's real numeric-repository `Link` continuation with an opaque `after` cursor: 155 issues and 344 pull requests over five pages matched across two passes. The two-pass `eng/probe-migration-project-read.fsx` diagnostic read the accessible live Coordination Project: 297 item identities over 3 pages, 24 field declarations over 1 page and 1,874 field values over 12 pages reconciled at snapshot SHA-256 `bd0ba1afb7af1c3f3600ac6725ab0c78635313b504f32faed62dc9d2139e8ba3`. The live organization Projects list also returned partial data with a `FORBIDDEN` error for another Project; this is why partial GraphQL data is rejected. These are diagnostic reads, not an isolated Project copy, full nine-authority discovery, or Q5/Q6 acceptance. Step 2's remaining surfaces, protected journal integration, write interpreter, and exact copy-specific report are pending; GS2-09.7 remains unregistered and unaccepted. The Project query follows [GitHub's GraphQL pagination contract](https://docs.github.com/en/graphql/guides/using-pagination-in-the-graphql-api) and [Project value schema](https://docs.github.com/en/graphql/reference/projects).

Status after native relation observation: `MigrationGitHubRead.readNativeRelations` consumes an exact terminal issue census, rereads each issue by GraphQL node ID, matches repository ID, number and `updatedAt`, and captures the raw selected relation payload with SHA-256. It distinguishes parent-to-child from blocker-to-blocked edges, checks every local edge against both endpoints, and retains cross-repository endpoints without claiming their external reciprocal was inspected. The three closed relation connections now follow `endCursor` to terminal pages, retain each continuation payload and digest, require a stable source identity, revision and total count, and reject missing, repeated, duplicate, changed or over-limit pages. This follows GitHub's documented [Issue relation fields](https://docs.github.com/en/graphql/reference/issues) and [GraphQL pagination](https://docs.github.com/en/graphql/guides/using-pagination-in-the-graphql-api). Independent controlled tests pass. The two-pass read-only `eng/probe-migration-native-relations.fsx` diagnostic saw 2 sandbox issues with no edges at SHA-256 `6473593fec88baf907ffa4e3ee82400be286c2fc6c35084271361462b0f4d3b2`; the public Coordination repository produced 155 issues, 12 distinct native edges including 5 cross-repository edges, at SHA-256 `743ba7e3df1d58f1672b9a74f34b3fb4c0c6adaad0f531b58f6a503edabca218`. A separate live read of Coordination issue #74 validated the `blocking(first:100,after:$after)` query shape by returning the three remaining edges after a one-edge cursor. These are provider diagnostics, not an isolated copy or a full nine-authority manifest. Cross-repository closure and the remaining authority surfaces still require copy-specific capture before acceptance. No live mutation or GS2-09.7 registration occurred.

Status after repository core settings observation: the read-only provider now captures one deliberately partial settings surface from the repository REST response. It pins the numeric and node identities, full name, default branch, visibility, archive/disabled/issue flags, merge modes and branch-deletion policy; it retains the exact response JSON and SHA-256. Black-box controlled tests require one GET and refuse identity drift, unknown visibility, missing or malformed fields, invalid options, and unavailable transport. This is a source preparation slice only: branch/tag rulesets, Actions policy, environments, security, releases, dependency controls, claims/events, review/delivery/release records, workflow pins and receiver identities still need their own complete reads and cross-surface reconciliation. The existing index has no GS2-09.7 unit or Q5/Q6 gate contract; `roadmap-work inspect` therefore returns `RW-UNIT-UNKNOWN`. No provider setting was changed and no representative-copy qualification is claimed.

The next read-only source slice captures the complete pull request population as a prerequisite for review, delivery and release records. It follows only in-scope `state=all` REST continuations to a terminal page, retains each requested URI and raw page digest, pins numeric/node PR identity, base repository and head/base revisions, and reconciles the final count against the independent issue endpoint's PR census from the same discovery pass. A missing or skipped page, off-scope continuation, changed population, duplicate identity, wrong base repository, malformed revision or nonterminal issue census refuses. The two-pass read-only `eng/probe-migration-pull-requests.fsx` diagnostic of public Coordination returned 155 issues over 6 issue pages and 363 pull requests over 4 PR pages with matching normalized SHA-256 `c2a8e4253ae8291536be9c50a0284ee12bf3fcd1549277533a673c960f883105`. This provider diagnostic is not an isolated copy or Q5/Q6 evidence. It does not yet capture review events, checks, delivery receipts, releases or tags, and the two-pass full authority snapshot remains pending.

A further read-only slice captures the terminal issue-comment stream for one issue already present in the complete issue census. It binds each comment to that exact issue URL, numeric/node identity, actor login, body and created/updated revisions, retaining raw comment and page digests. Missing, skipped or cross-subject pages, duplicate comments, unavailable bodies and uncensused issues refuse. The two-pass `eng/probe-migration-issue-comments.fsx` diagnostic of public Coordination issue #74 observed 9 comments on 1 terminal page, bound to a 155-issue census, with matching normalized SHA-256 `d89f59af5fbca0a67c3a355fa15a7a4cb25ddce388766bab1fde22fe7d5191b6`. This is one issue's comment input, not a full claim/event stream: every censused issue and PR, non-comment events, protected journal records and legacy custom receipts still need capture and reconciliation before any migration claim.

The same censused-issue boundary now reads native issue events through terminal REST pagination, retaining raw event and page digests, event kind, revision and optional system actor. Missing, skipped or cross-subject pages, duplicate events, malformed actor identity and uncensused issues refuse. The two-pass `eng/probe-migration-issue-events.fsx` diagnostic of public Coordination issue #74 observed 17 events on 1 terminal page with matching normalized SHA-256 `0c86da150a43a81910494ca24e47677361c64d32aebdac8203ba810a2deb57dd`. This is one issue's provider diagnostic, not a complete fleet event stream, custom receipt reconstruction or isolated-copy rehearsal.

Pull request lifecycle comments use GitHub's issue-comment endpoint, so the same terminal, raw-page-bound reader now accepts a PR only when its number and node ID are present in the complete PR census. A foreign comment subject, missing continuation, uncensused PR or nonterminal PR census refuses before any positive stream result. The two-pass read-only `eng/probe-migration-pull-request-comments.fsx` diagnostic of public Coordination PR #513 observed 5 comments on 1 page after reconciling 363 PRs against the issue census; the normalized SHA-256 was `d1329f4799fe609c0d365b001e5e7f16727ccf83f743641b70caac03832f55a9`. This is one PR's comment input, not the whole review/delivery/release authority or Q5/Q6 evidence.

Native PR reviews and inline review comments now have separate terminal, raw-page-bound readers, both keyed to an exact PR census. The review reader retains state, optional actor/commit/submission facts and raw payload; the inline-comment reader retains path, body, optional parent review ID, revisions and raw payload. Controlled responses prove positive nonempty reads and refuse wrong PR URL, unknown state, invalid parent review, duplicates, missing continuation and nonterminal source census. The two-pass read-only `eng/probe-migration-pull-request-reviews.fsx` diagnostic of public Coordination PR #513 observed terminal empty review and inline-comment pages with normalized SHA-256 `9f2917753efce258ed747657db54f1ffa158b8a8cd3d99e1295225815f20c694`. That diagnostic proves an empty provider response for this subject, not nonempty provider correspondence, complete review history, delivery/release records or Q5/Q6.

`MigrationNativeActivity.capture` now walks all five native activity streams for **every** issue and PR in its initial terminal censuses, then rereads both censuses and refuses population drift. `captureStable` performs two such full captures and requires exact input and digest agreement. The issue reader retains each requested page URI, raw response digest and continuation; the pure `reconcile` boundary requires terminal linked page proofs for both censuses and every stream, one stream per censused subject, exact subject and repository binding, raw item digest agreement, unique native node IDs, and valid inline review parents before producing one activity digest. Controlled tests cover empty and nonempty provider captures with GET-only requests, a newly added source issue, changed raw issue page across full passes, missing issue-page proof or stream, broken terminal pages, tampered payload, wrong subject, duplicate node and orphan review comment. The resulting digest covers native comments, issue events and reviews only. It does not include protected journal or custom receipts, checks, workflow delivery, releases, tags, settings, Project state, or a full two-pass copy-specific authority snapshot; no live all-subject activity capture was run.

### Isolated-copy capability audit (2026-09-24)

Read-only provider metadata identifies `FS-GG/FS.GG.GitHub.Substrate.Sandbox` as the previously used private diagnostic repository (numeric ID `1353050537`, node `R_kgDOUKXpqQ`). It is not a declared GS2-09.7 migration copy. The current user actor is `EHotwagner` (ID `1645484`); repository metadata reports `admin`, `maintain`, `push`, `pull` and `triage` as true for that actor. Those broad effective repository flags do not establish a scoped mutation credential or permission to bypass protected journal rules. The formerly proposed `FS-GG/FS.GG.Coordination.CallableSandbox` returned HTTP 404 on this audit; an earlier GS2-09.9 native-operation receipt for that name is historical evidence, not a present target reservation.

The only Project returned with an accessible identity was the **live** FS-GG Coordination Project, number `1`, node `PVT_kwDOEYAWY84Bb08W` (`https://github.com/orgs/FS-GG/projects/1`). The organization list also returned one node with `FORBIDDEN` and partial data; Project number `12` returned `NOT_FOUND`. Neither result identifies an isolated Project copy. No exact GS2-09.7 copy repository set, Project node ID, source/target heads or snapshot digests is bound. A `FORBIDDEN` or `NOT_FOUND` result cannot be interpreted as absence of an isolated copy or authority.

The production Authority repository is `FS-GG/FS.GG.Coordination.Authority` (numeric ID `1351660651`, node `R_kgDOUJC0aw`) and has active journal and phase rulesets. The GS2-08.2 desired policy names separate ordinary and cutover contents-only writer Apps (`4882140`/installation `160261608` and `4882399`/installation `160261436`), but records both as `bound-awaiting-fresh-readback`. This audit did not obtain either App credential, verify current installation grants, or identify a separate protected journal writer for an isolated GS2-09.7 cohort. A user-token repository `admin` flag is no substitute for the exact protected-ref CAS identity.

**Concrete cohort request before Q5/Q6 execution:** identify disposable isolated repository copies by full name, numeric and node IDs, expected source and target head SHAs, and complete snapshot digests; identify a disposable Project copy by organization, number and node ID with its field and item census; identify the actor and distinct scoped credentials for repository issue/type/relation/body writes, Project writes, repository settings, receiver pin and schedule changes, archive publication, and protected journal CAS on a copy-specific ref. Supply exact selected-repository and Project grants, protection/bypass readback, allowed effects, cleanup ownership and expiration. [GitHub's Project-copy behavior](https://docs.github.com/en/issues/planning-and-tracking-with-projects/creating-projects/copying-an-existing-project) carries views, fields and optionally draft issues, but excludes the original items, collaborators, and repository/team links. The cohort therefore also needs its disposable repository issues/PRs attached to the copy, their field values reconstructed, and the exact Project item/field/value census captured after that work. A copied Project shell alone is insufficient. Current observations establish none of those copy-specific mutation grants. The live Coordination Project and production Authority journal are observation subjects only. Until a copy cohort and its scoped authorities are bound and reread, Q5/Q6 is blocked; source-only tests and diagnostics remain preparation, and GS2-09.7 remains unregistered and unaccepted.

### Authority and execution path to a copy-specific command

The accepted `GitHubCompleteDiscoveryQualification.expectedAuthorities` contract requires the following nine complete authorities in **each** quiescent pass. The listed adapters are reusable contracts; none makes the current partial provider capture a complete authority.

| Discovery authority | Current provider read / reusable adapter | Missing copy-specific binding before manifest or effect |
| --- | --- | --- |
| `issues-open-and-relevant-closed` | `MigrationGitHubRead.readIssues` now retains terminal issue-page proofs and issue/PR census; `IssueFields` models type/field plans. | Select relevant closed scope, source bytes and high-water mark; bind exact issue/type/field state and target readback. |
| `project-items`, `project-fields` | `readProjectItems`, `readProjectFields`, `readProjectValues` and `reconcileProject` cover item, schema and typed value pages; `ProjectAdapter` models membership and projection. | Bind an isolated Project ID; attach the disposable repository issues/PRs omitted by GitHub's Project copy, reconstruct field values, then prove exact item, schema, value and cross-repository content closure before `SetProjectField`. |
| `hierarchy-and-dependencies` | `readNativeRelations` observes terminal local relation pages; `NativeRelations` plans and verifies edges. | Close cross-repository endpoints; supply provider writes and readback for parent and blocking add/remove operations. The current step union only has `AddBlockingEdge`. |
| `claim-and-event-streams` | `MigrationNativeActivity.captureStable` reads native comments/events twice; `ClaimTouchSetAdapter` and `ShardedJournalAdapter` model adjacent authorities. | Capture and reconcile legacy claims, custom receipts and protected claim/operation journal histories; native activity alone cannot reconstruct them. |
| `review-delivery-release-records` | Native PR comments/reviews/inline comments are read; `ReviewDeliveryAdapter` and `ActionsReleaseFeedAdapter` provide adjacent contracts. | Capture checks, merge/delivery receipts, release/tag records and their journal correspondence with terminal pages and two-pass identity. |
| `repository-settings` | `readRepositoryCoreSettings` captures one partial REST response; `RepositorySettingsAdapter` defines 11 surfaces. | Read and classify every applicable settings surface, including rulesets, environments, Actions and security; bind prestate/desired plans and provider readback. |
| `workflow-pins` | No migration provider reader; receiver/tool manifests exist outside this capture. | Pin workflow and package bytes/heads from the isolated copy and prove the exact intended receiver change. |
| `receiver-identities` | `GitHubImmutableManifest` has `ReceiverHeads`; `AdoptReceiver` is a sealed effect shape. | Read exact receiver repository IDs, heads, trees and pins; bind scoped copy receivers and the provider write/readback path. |

The shortest vertical source route is one command with explicit `inspect` and `execute` phases. `inspect` must reject any missing row above, bind the two complete provider passes to fresh GS2-09.1 discovery, then produce the copy-specific GS2-09.2 manifest, GS2-09.3 transform decisions and GS2-09.4 operation dispositions. The accepted qualification contracts already define their seals and refusal controls; no migration command composes them from provider reads today. A thin first implementation may accept only a declared, fully observed subset of effects and refuse every unsupported manifest operation, but it cannot label that subset as full Q5.

`execute` must bind `MigrationStepExecution.advance` to a real `IMigrationStepRuntime`: copy-specific epoch/fence and target observations, protected expected-parent CAS journal, pre-dispatch grant, typed provider dispatch, lost-response recovery and authoritative result readback. Its seven current effect cases omit body-metadata removal, parent-edge and blocking-edge removal, scheduling, v1 projection and authority-snapshot restoration. The GS2-09.5 sealed history and GS2-09.6 rollback-plan contracts have no copy-specific archive publisher/verifier or reverse-order restoration interpreter. A Q5/Q6 command must add those integrations, run every interruption cut and fresh-process restart, verify the archive, restore five rollback domains, then perform a distinct second migration round with retained request/receipt counts and independent refusal controls. The protected copy cohort and scoped credentials above remain an external prerequisite for execution.

The pure `GitHubMigrationInspect.inspect` source boundary now requests the nine named authorities in each of two ordered passes. The cohort digest binds isolation, source revision, Project organization/number/node ID and every repository numeric/node ID plus source/target head. Each returned authority must match that digest and its own name; its retained raw page bytes must hash to the claimed SHA-256, form a terminal linked request-identity chain, and account for the exact typed subject list. Request identity supports distinct GraphQL cursor pages at one URI. Page proofs must agree across passes before the GS2-09.1 discovery contract can qualify. The boundary then demands full GS2-09.2 manifest, GS2-09.3 transform and GS2-09.4 operation artifacts from explicit stage builders; it verifies each artifact and its exact predecessor digest/seal links, and refuses a missing builder or changed binding. Controlled tests prove refusal for a live cohort, absent authority, missing or altered raw page, changed page across passes, unqualified adapter and absent manifest builder. `ScopeVerified` and `SubjectsParsedFromRaw` are assertions made by a future adapter, not independent provider proof: that adapter must have its own exact-scope parser and refusal controls so a coherent foreign page cannot masquerade as this cohort. This is an inspect composition contract, not a live command or provider correspondence: no production adapter supplies all nine authorities or the copy-specific stage builders, and a controlled in-memory source cannot stand in for them.

The draft CLI-owned `MigrationInspectProviderAdapter` now gives the issue census, Project item membership, Project field declarations and per-item values a narrow raw-to-typed binding. It independently parses captured response bodies, checks exact repository/Project identity and typed records, reconciles issue/PR markers and page counts, checks terminal continuation chains, and requires each Project reader's exact canonical GraphQL document, variables and cursor sequence. Project item and value pages form one linked `project-items` proof; their distinct subjects are sorted and the value pages contribute to the high-water digest. Before that proof is returned, the adapter reads the field census and runs `reconcileProject`, refusing unknown field IDs or changed item revisions. The later `project-fields` authority must match the exact field-page fingerprint from that same pass and cohort, and a pass without its own item/field proof refuses. The capture set is closed: an extra REST or GraphQL call refuses. A pre-dispatch transport fence allows only repository/issue GETs or the exact three Project queries; a write-shaped call never reaches the inner transport. Controlled negative cases cover foreign identity, altered typed records, missing terminal pages, partial GraphQL errors, forged query text, wrong variables, nested truncation, unknown fields, field-proof drift and extra calls. The remaining six discovery authorities return `authority-adapter-unavailable`; native relations and other partial readers have no raw-to-typed inspect adapter yet. These tests establish the adapter's controlled contract only. There has been no isolated-copy provider run, no all-nine inspect result, no copy-specific GS2-09.1–.4 artifacts, and no Q5/Q6 acceptance claim.
