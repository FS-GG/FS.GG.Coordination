# Closed v5 artifact and workflow source witness

Status: source-only fake-port candidate stacked on draft #773. The [witness](../../eng/callable_isolated_v2_v5_artifact_workflow_witness.py) compares the closed #768 selection and #773 installed refusal result with separate injected artifact and workflow observations. The artifact record must bind the selected repository, revision/tree, producer run and attempt, producer actor, artifact ID, exact #764 archive bytes and manifest digest. The workflow record must bind the same revision/tree and exact disabled execution workflow bytes. The two readers have separate scoped fake principals and credentials, also separate from the earlier source and review readers. Success remains `authorized=false`, `can_dispatch=false`, `live_effects=0`.

The [tests](../../eng/tests/callable-isolated-v2-v5-artifact-workflow-witness/test_v5_artifact_workflow_witness.py) began red because the witness module was absent. Negative controls reject foreign producer run/attempt/actor or artifact ID, changed archive bytes, foreign workflow tree or bytes, mutable records, shared readers, mismatched installed result and expired artifact. The qualification call performs no file, token-environment, socket or SQLite access.

These fake records cannot prove a protected producer run or Git tree membership. An owner must independently select the immutable integrated source and workflow head, authenticate producer and reviewer event identities, fetch the immutable provider artifact and verify the Git tree and manifest, then independently observe the protected runner image/runtime and installed no-grant refusal. This source does not enable either workflow or install the ZIP. A separate later #550 decision remains necessary for protected target/App/vault/CAS/grant custody and one native POST with complete readback. #545 receipt, protected merge, Authority write and cutover remain held.
