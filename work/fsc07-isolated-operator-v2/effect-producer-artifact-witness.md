# Closed effect scaffold producer and artifact witness

Status: source-only, non-authorizing fake-port contract. The [release preflight](effect-release-preflight.md) now retains its checked producer run ID, attempt, actor, reviewer actor and approval event in the closed result. The [Git-tree witness](effect-git-tree-witness.md) separately returns the selected commit/tree and all seven source-file digests. This witness requires both results to agree on revision, tree, archive and repository-identity event before reading producer objects.

`eng/callable_isolated_v2_effect_producer_artifact.py` takes one selected Coordination repository ID, repository-identity event ID, producer run/attempt/actor, producer workflow ID/path/hash and artifact ID. An injected read-only producer port returns a completed successful `workflow_dispatch` run on exact `main` head and tree, plus an unexpired artifact record for the same run/attempt/workflow. A separately scoped bundle reader downloads the selected artifact URL. The raw bundle size and SHA-256 must match the producer artifact record; it must contain exactly one regular-file closed scaffold archive whose SHA-256 matches the release preflight. The producer and bundle readers must have distinct principal and credential claims, with exact read-only scopes unchanged before and after. The version 4 closed result carries the checked repository ID and identity-event ID, producer actor and selected source record ID for the later reviewer join. It remains `authorized=false`, `can_dispatch=false`, `live_effects=0`.

The [fake controls](../../eng/tests/callable-isolated-v2-effect-producer-artifact/test_effect_producer_artifact.py) failed red before the module existed. A second red control demonstrated that a digest-consistent ZIP containing the right archive bytes in a symlink-mode member previously passed; the source now refuses that mode. Later red-before controls showed that either the producer reader or bundle reader could return one shared mutable scope dictionary, change its credential during download, and still pass the before/after comparison. The validated scopes are now deep-copied. Other controls invert head revision/tree, run attempt/actor/status, workflow ID/hash, artifact metadata/digest/URL, bundle bytes, source-tree completeness, reader custody/scope and time. The positive join denies local file, environment-token, socket and SQLite access while qualifying. These are fake records; no protected run, artifact download or independent App identity has been observed.

The selected producer workflow path, `.github/workflows/callable-isolated-v2-effect-release.yml`, has a [disabled source candidate](effect-producer-workflow-candidate.md) in this stacked draft. The producer witness requires a separate [workflow-source result](effect-producer-workflow-source.md) that binds its selected hash to that path in the exact Git tree. Red-before controls showed that a constructed result with a coherent foreign workflow digest or blob ID could pass this downstream join; both are now pinned to the disabled candidate's exact values. A protected owner must independently review its later integrated source commit, authenticate the Git/run/artifact/download readers, and obtain independent release approval. No protected producer run or artifact exists. This witness alone does not approve the manifest, install the archive, issue a grant or permit a provider effect. Both proposed workflows remain disabled; #545 disputed receipt and the [#550 native one-attempt gate](https://github.com/FS-GG/FS.GG.Coordination/pull/550) remain held. No credential, journal/CAS, POST, Authority write or cutover is authorized.
