# Closed v5 runner issuer event witness

Status: source-only fake-port candidate stacked on draft #786. The [installed refusal result](../../eng/callable_isolated_v2_v5_installed_refusal.py) now carries the selected runner run attempt. The [install approval result](../../eng/callable_isolated_v2_v5_install_approval.py) carries its fake approval reader identity. The [issuer witness](../../eng/callable_isolated_v2_v5_runner_issuer.py) compares a separately selected fake issuer event with the exact source tree, artifact, install approval event, runner run and attempt, actor, image/interpreter/runtime digests and install path. It requires a distinct issuer actor and tenth distinct fake reader identity, narrow read-only scope and event issuance before the no-grant command. Success remains `authorized=false`, `can_dispatch=false`, `live_effects=0`.

The [tests](../../eng/tests/callable-isolated-v2-v5-runner-issuer/test_v5_runner_issuer.py) began red because the adapter was absent; a separate red test showed the installed result omitted run attempt. Negative controls reject wrong run attempt, image/runtime/path, approval event, self issuance, postcommand or expired event, reused reader, broad scope and any file, token-environment, socket or SQLite access during qualification.

The issuer event and its actor remain fake. Actual protected runner identity requires an authenticated immutable issuer/event source, selected image and runtime attestations, independently fetched producer artifact, and installed no-grant refusal observation. Both workflows stay disabled; no protected install, grant or native effect occurred. #550 one-POST and #545 receipt, protected merge, Authority write and cutover remain held.
