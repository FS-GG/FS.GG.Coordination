# Closed effect scaffold installed-refusal readback contract

Status: source-only, non-authorizing. The [protected owner packet](effect-release-owner-evidence.md) has no selected integrated revision, producer artifact, independent approval event or runner. Both proposed workflows remain disabled.

The [release preflight](effect-release-preflight.md) now carries the verified archive SHA-256 into its closed result. `eng/callable_isolated_v2_effect_runner_readback.py` takes that result, an exact run/attempt, runner actor, audit event, immutable image and attestation digests, interpreter and runtime-closure digests, and an absolute canonical install path. It compares two injected read-only observations:

1. A runner probe with the same source revision/tree, manifest, artifact and archive hash; exact installed archive bytes; the same before/after device, inode, size and hash; no symlink claim; the pinned `python3 -I -S` argv; exit 78; and the exact `no-grant` JSON line with empty stderr.
2. A separately read audit event with the same run, artifact, install path and file object; a distinct observer principal, credential and actor; and zero token reads, journal reads/writes, CAS writes, provider POSTs and cleanup attempts.

The runner and audit scopes are checked before and after reading. Missing fields, broad scope, foreign identities, stale times, malformed counters, path aliases, changed archive bytes or object identity, and any nonzero effect count refuse. The output remains `authorized=false`, `can_dispatch=false`, `live_effects=0`. Tests use fake ports and deny local file, environment-token, socket and journal access while qualifying.

This source join cannot authenticate a fake port's claims, prove a runner image or interpreter digest, detect a transient ABA mutation between the before/after observations, or prove external counters from a real protected host. A protected owner must supply immutable source/producer/approval identities, independent reader credentials, a pinned runner image and runtime closure, and an independently observed no-effect probe under a separate installation decision. The closed artifact has no runnable native effect; a future runnable artifact, selected target, credential custody, CAS journal, grant and one-POST authorization remain a distinct [#550 native gate](https://github.com/FS-GG/FS.GG.Coordination/pull/550). #545's disputed receipt, typed gate/index and cutover remain held.
