# Closed v5 issuer public-key registry witness

Status: source-only fake-port candidate stacked on draft #794. The [envelope result](../../eng/callable_isolated_v2_v5_issuer_envelope.py) now carries its selected nonce and fake verifier-reader identity. The [registry witness](../../eng/callable_isolated_v2_v5_key_registry.py) joins that exact envelope digest and nonce to a separate key-registry event, selected run/attempt, issuer event/actor, install approval, source revision/tree and artifact. It requires 32 public-key bytes whose SHA-256 equals the selected key ID, `Ed25519`, the exact no-grant audience, active status, no revocation, a distinct reviewer/reader, narrow read-only scope and registry observation before issuer signing. Success remains `authorized=false`, `can_dispatch=false`, `live_effects=0`.

The [tests](../../eng/tests/callable-isolated-v2-v5-key-registry/test_v5_key_registry.py) began red because the registry adapter was absent; a separate red test showed the envelope result lacked nonce and verifier-reader fields. Negatives reject wrong public key, actor, run attempt, envelope, approval or nonce, revoked/expired records, late observation, self review, reused verifier reader, broad scope and any file, token-environment, socket or SQLite access during qualification.

This registry and its key are **fake**. Matching a SHA-256 fingerprint does not establish trust in the key, prove the key is a valid Ed25519 point, or authenticate the issuer/signature. A protected owner must approve an immutable trust root, independently read and authenticate a real registry event and revocation state, and run a real signature verifier against that approved public key. Replay-resistant nonce custody, integrated source/artifact, runner image/runtime and actual installed refusal remain separate admission facts. Both workflows stay disabled; #550 native one-POST, #545 disputed receipt, protected merge, Authority write and cutover remain held.
