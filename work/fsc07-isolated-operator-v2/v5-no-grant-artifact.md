# Local v5 no-grant refusal archive candidate

Status: deterministic source/test candidate, stacked on [#761](https://github.com/FS-GG/FS.GG.Coordination/pull/761). The [builder](../../eng/build_callable_isolated_v2_v5_no_grant.py) makes a separate ZIP app with exactly one member, `__main__.py`, from the [closed v5 vault entry](v5-vault-no-grant-entry.md). It contains no native operator, token reader, vault client, journal writer or provider transport. It does not replace the current closed effect archive or enable either disabled workflow. No archive is committed or protected-installed by this draft.

The [committed manifest](v5-no-grant-manifest.json) records the exact entry source, disabled execution workflow, builder source and archive bytes. In this source revision, the local archive SHA-256 is `a8b3bdc58f220061bb2e2e115d6aa0d042facd9881323a77dfe884b3b461f05c`; the manifest-file SHA-256 is `f1cd7456e66bede7891e6295eb0437916d66d1f450dcdb12b0af141ddffa5e30`. The [pure verifier](../../eng/verify_callable_isolated_v2_v5_no_grant.py) requires a separately supplied approved manifest digest, exact builder/entry/workflow bytes, canonical manifest JSON and byte-identical deterministic ZIP layout. Supplying a matching digest in a local test is not protected approval.

The [controls](../../eng/tests/callable-isolated-v2-v5-no-grant-artifact/test_v5_no_grant_artifact.py) first failed because the builder was absent. Two independent local builds then matched exactly. Source or workflow mutation refuses before writing an archive. Archive trailing bytes, wrong member source, changed builder or entry bytes, changed workflow, malformed manifest and wrong approval digest refuse. A clean-directory `python3 -I -S` run exits 78 with absent or extra grant arguments; token and journal sentinel files remain unchanged, and an audit hook refuses their read or a socket/SQLite connection. These are local negative controls only; they do not prove protected installed path, runner/image/runtime, independent audit event or token custody.

The next protected step still requires a distinct owner and reviewer to select an integrated source/workflow head, immutable artifact and approved manifest digest, then independently verify installation and no-grant counters. A separate later decision must authorize target/App scope, actual handle-to-token mapping, durable CAS, one-use grant and at most one provider POST with complete native readback. This candidate cannot dispatch even if installed. [#550](https://github.com/FS-GG/FS.GG.Coordination/pull/550) native effect, [#545](https://github.com/FS-GG/FS.GG.Coordination/pull/545) disputed receipt, protected merge, Authority write and cutover remain held.
