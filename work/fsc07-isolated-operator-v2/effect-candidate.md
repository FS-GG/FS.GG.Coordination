# Source-only v2 native effect candidate packet

Status: source and synthetic tests, 2026-09-25. Stacked on the [native custody decision](native-custody-decision.md). This is a narrower prerequisite than a runnable effect artifact or workflow: the separate #555–#573 release stack is inspect-only, #563 is disabled, and no protected observer, selected target, execution token or CAS backend has been installed here.

[`eng/callable_isolated_v2_effect_candidate.py`](../../eng/callable_isolated_v2_effect_candidate.py) checks a canonical versioned `prepared-not-authorized` packet against a separately supplied expected object. It binds a Coordination revision/tree and operator/control hashes; a future effect archive and exact `.github` workflow revision/path/hash; immutable image, attestation, interpreter and closure hashes; protected run/attempt, distinct actor and reviewer, source-only review event and expiry; one selected repository and installation with exact refs/prestate; and a single canonical PR POST intent. It refuses the #555 inspect-only archive as an effect artifact, zero or mutable pins, malformed/duplicate/noncanonical JSON, extra grant fields, expired or self-reviewed source review, broad method/path/write count, and any changed expected binding. The expected object is caller-supplied consistency data, not a protected observation.

The module imports no token, HTTP, journal or CLI adapter. Its only successful result has `authorized:false`, `can_dispatch:false`, `live_effects:0`. The [independent synthetic tests](../../eng/tests/callable-isolated-v2-effect-candidate/test_effect_candidate.py) mutate each pin and identity, stale review, wrong effect, extra/missing fields and input encoding. The matching control patches token environment lookup, file opening, sockets and SQLite to fail, then still returns closed. Those tests prove source behavior only; no candidate archive is built and no protected workflow or installed command is enabled.

The next implementation must integrate a separately reviewed effect artifact and protected `.github` workflow, with authenticated source/release, review, target and App scope observations, one-use grant issuance, protected CAS marker/readback and independent native poststate. A later source gate must prove no-grant zero token read/CAS/POST through the installed command. Only after those bytes and a fresh target/setup receipt are protected and independently read back can an operation reviewer approve the exact run/attempt and issue a short-lived grant before at most one POST. The current GS2-09.9 typed ceiling, gate/index, #545 receipt, #563 workflow, receiver and cutover remain held.
