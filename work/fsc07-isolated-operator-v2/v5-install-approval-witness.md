# Closed v5 install approval event witness

Status: source-only fake-port candidate stacked on draft #779. The [installed refusal result](../../eng/callable_isolated_v2_v5_installed_refusal.py) now carries runner actor, image/interpreter/runtime digests, install path and command interval. The [approval witness](../../eng/callable_isolated_v2_v5_install_approval.py) compares those fields with a separately selected fake install-review event, the exact #764 artifact/manifest, and the five-file #779 Git tree witness. It requires a distinct reviewer actor, narrow read-only scope, a no-grant-install decision observed before the command, and unexpired prior source review. Success remains `authorized=false`, `can_dispatch=false`, `live_effects=0`.

The [tests](../../eng/tests/callable-isolated-v2-v5-install-approval/test_v5_install_approval.py) began red because the approval adapter was absent; a separate red test showed the installed readback lacked runner fields. Another red control showed that an expired earlier selection review could still pass. Tests now reject wrong tree identity/file set, artifact, runner image/runtime/path, self review, postcommand or expired approval, broad scope, wrong event and expired prior review. The qualification call performs no file, token-environment, socket or SQLite access.

The selected event, reviewers, tree witness and installed readback remain fake. This does not issue protected approval, enable either disabled workflow or install the ZIP. The first protected owner step remains independent selection of an immutable integrated source/workflow and actual producer artifact with distinct authenticated reviewer, runner image/runtime and installed refusal evidence. A separate later #550 authorization must bind target, App scope, vault/token custody, journal CAS and one native POST with complete readback. #545 disputed receipt, merge, Authority write and cutover remain held.
