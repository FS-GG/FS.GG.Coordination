# Closed effective-scope and prestate join

Status: source-only fake-port contract, 2026-09-25, stacked on [#602](https://github.com/FS-GG/FS.GG.Coordination/pull/602). The [join](../../eng/callable_isolated_v2_effective_scope_prestate_join.py) supplies the effective-scope witness shape consumed by the [#600 credential record adapter](app-credential-record-adapter.md). It has no protected metadata reader implementation, execution token, HTTP client, journal, grant or dispatch entry.

The caller selects one repository, installation, refs and SHAs, prestate digest, run ID and attempt. A distinct injected [#602 prestate reader](target-prestate-read-adapter.md) must return a fresh complete observation for that selection. The injected metadata reader claims only access to `coordination-effective-app-scope` and returns canonical token-free bytes. The join requires one App ID, installation, repository ID, exact permissions, credential ID, mint-record digest, target digest, four native response digests, prestate record/transcript/digest, run and attempt. It checks issued, prestate, effective observation and expiry order, a five-minute prestate window, and stable metadata-reader scope before and after the read. A boolean repository ID is refused even where Python would compare it equal to integer `1`.

The [fake controls](../../eng/tests/callable-isolated-v2-effective-scope-prestate-join/test_effective_scope_prestate_join.py) close file, token environment, socket and SQLite access; they cover foreign or replayed prestate, broad or foreign App scope, stale or malformed metadata, changed reader scope, duplicate JSON and sanitized read failures. The boolean-ID control failed before the exact-type repair. These controls establish only parser and join behavior with injected values.

The prestate reader now carries the witness principal, credential and time window through to this join. It refuses reuse of either reader identity as the witness, stale witness time and witness expiry before the effective observation. A red-before control showed these fields were previously absent. The protected owner still needs an independently authenticated metadata reader, a provider-backed effective installation-token scope observation, and independent readback of the witness's immutable event and producer. These source-level identity fields cannot prove custody. Neither the selected target nor the fake port is an authorization grant. #563 remains disabled; the #550 native one-attempt target, credential, journal CAS, grant, effect and complete readback remain held. The #545 receipt, gate/index, Authority boundary and cutover remain unchanged.
