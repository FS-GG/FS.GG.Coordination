# Closed v5 native-effect port proposal

Status: source-only type proposal, 2026-09-25. The [module](../../eng/callable_isolated_v2_effect_v5_ports.py) names the evidence and port roles needed by the [#550 v5 authority amendment](https://github.com/FS-GG/FS.GG.Coordination/blob/19323acc60f6f1314b9c92030c203bf2789e21ad/work/gs2-09-9-isolated-operator-rotation/authority-amendment.md). It is not packaged in the closed archive and implements no observer, grant verifier, token reader, CAS, HTTP transport or native dispatcher. Its only callable entry, `inspect_closed`, ignores all supplied coordinates, ports and grant bytes and returns exit 78 with `authorized=false`, `can_dispatch=false`, `live_effects=0`.

| Proposed type or port | v5 binding reserved | Missing protected implementation |
| --- | --- | --- |
| `ReleaseCoordinates`; source and approval readers | Integrated Coordination commit/tree, operator, v5 contract/proposal, controls, archive, installed entry, conditional package bytes, protected workflow, producer/artifact/reviewer and runner/runtime pins. | Authenticated Git objects, producer run/artifact, immutable review event and installed observer. |
| `NativeCoordinates`; target and App-scope readers | One operation/request, target repository and refs, setup receipt, exact execution run/attempt/reviewer/issuer/grant, journal location and parent generation. | Separately admitted disposable target, effective one-repository App scope, active reviewer and one-use grant. |
| `GrantRecord`; separate grant and issuer readers | Canonical artifact/payload digests, exact run/attempt and approval event, distinct issuer and expiry. | Protected grant verification, immutable issuance event and replay custody. |
| `JournalRecord`; separate replay reader and writer | Prior and committed generation/head, request/target/grant digests and `attempt-may-have-started`. | Protected atomic CAS backend, distinct writer and fresh-process replay reader. |
| Separate execution-token and provider-writer ports | Selected App token and the sole canonical PR POST. | Credential custody, installed grant verification and one-attempt authority. No implementation is supplied here. |
| `NativeResultRecord`; readback reader | Response status, counted POST and two complete snapshot digests. | Independent native repository/ref/PR list/detail observation and no-repeat proof. |

All records are caller-supplied type shapes, not authenticated observations. The [controls](../../eng/tests/callable-isolated-v2-v5-closed-ports/test_v5_closed_ports.py) failed red before this module existed, then proved that absent and untrusted grant bytes, including an object that refuses attribute access, trigger no port, file, token-environment, socket or SQLite access. A later red-before control showed that callers could construct or replace a `ClosedV5Decision` with `authorized=true`, `can_dispatch=true` or exit 0; the closed status fields are now fixed outside its constructor. The controls also supply one shared fake object for every proposed role and still obtain only refusal. This proves no authority follows from a role collision in the closed scaffold; it does not attest distinct protected principals or credentials. The module stays outside the current archive and both proposed workflows retain `if: ${{ false }}` plus exit 78. The existing [closed entry](../../eng/callable_isolated_v2_effect_entry.py) and [archive manifest](effect-scaffold-manifest.json) are unchanged.

The [admission matrix](native-effect-admission-matrix.md) lists the independent selected facts and installed negative controls before a future runnable revision can be considered. The smallest missing source step is a separate reviewable runnable revision that binds these ports to authenticated evidence while retaining no-grant refusal. That revision needs exact protected identities and a selected integrated source/workflow decision; no such selection exists. A source proposal alone cannot issue a grant or open #550's POST window. Both workflows, #545 disputed receipt, typed gate/index, Authority boundary and cutover remain held.
