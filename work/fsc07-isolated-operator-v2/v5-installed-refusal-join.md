# Closed v5 installed refusal observation join

Status: source-only fake-port candidate, stacked on draft #768. The [selection result](../../eng/callable_isolated_v2_v5_no_grant_selection.py) now carries selected repository/artifact, producer/reviewer, source record, review time, and source/review reader identities into the [installed refusal join](../../eng/callable_isolated_v2_v5_installed_refusal.py). An injected runner probe and separate no-effect audit must agree on those coordinates, the #764 pinned archive and manifest, runner image/interpreter/runtime digests, canonical install path, file object before and after the command, exact `python3 -I -S` argv, exit 78 refusal bytes, and zero access/effect counters. Source, review, runner, and audit readers have distinct fake principals and credentials. Success remains `authorized=false`, `can_dispatch=false`, `live_effects=0`.

The [tests](../../eng/tests/callable-isolated-v2-v5-installed-refusal/test_v5_installed_refusal.py) began red with the absent join. A further red test showed a reader could mutate the supposedly frozen selection object during a probe to make a different artifact ID pass. The join now snapshots and checks the selection after both reads. Another red test showed runner-reader reuse of the source-reader identity; this now refuses. Other negatives cover foreign runner image/path/artifact, changed archive bytes, path replacement, wrong refusal output, self audit, stale observation and nonzero provider calls. The qualification call has no file, environment-token, socket or SQLite access.

All inputs are local fake records. Their producer, reviewer, image, runtime, installed path and audit identities are not authenticated or independently protected. A protected owner must select an immutable integrated head and disabled workflow, verify an immutable producer artifact and manifest, bind a separate review event and runner image/runtime, then independently observe actual installed refusal and zero access counters. This draft does not install anything or enable either workflow. A later, separate #550 owner decision must admit target/App/vault/CAS/grant custody and one native POST with complete readback. #545 receipt, protected merge, Authority write and cutover remain held.
