# Closed v5 ZIP candidate selection join

Status: source-only fake-port qualification, stacked on draft #764. The [adapter](../../eng/callable_isolated_v2_v5_no_grant_selection.py) joins one selected Coordination repository ID, revision/tree, producer run and artifact ID, source observation ID, and separate reviewer event ID. Both injected observations must bind the same exact six SHA-256 values. The adapter independently pins those values to the #764 refusal ZIP, manifest, builder, verifier, entry, and disabled execution workflow, then calls the canonical ZIP byte verifier. Its only successful result has `authorized=false`, `can_dispatch=false`, and `live_effects=0`.

The [negative tests](../../eng/tests/callable-isolated-v2-v5-no-grant-selection/test_v5_no_grant_selection.py) began red because the adapter did not exist. A second red control showed that matching fake observers could otherwise bless foreign verifier bytes. Tests now refuse swapped source/review coordinates, changed bytes, self review, stale events, broad or shared reader scope, scope drift, and bool/int aliases. The qualification call performs no file, environment-token, socket, or SQLite access. The read ports are local fakes; their identities, event times and repository objects are not authenticated.

This is **not** protected release approval or installed provenance. A protected owner still must select an immutable integrated source/workflow revision, independent producer and reviewer principals, immutable runner/runtime and artifact, approved manifest digest, and independently observed installed refusal. The local #764 archive contains only a no-grant entry and cannot dispatch. A separate later #550 admission must identify the protected target, effective App permission, vault handle/token custody, durable journal CAS, one-use grant, one POST boundary, and complete native readback. #545 receipt, both disabled workflows, protected merge, Authority write, and cutover remain held.
