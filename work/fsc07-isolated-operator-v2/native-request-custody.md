# Closed v5 native request custody join

Status: source-only fake-record proposal, stacked on [#745](https://github.com/FS-GG/FS.GG.Coordination/pull/745). The [join](../../eng/callable_isolated_v2_native_request_custody.py) compares the same canonical request bytes from the [sealed plan](operation-plan-read-adapter.md), the [seal event witness](plan-seal-event-witness.md), the [journal intent readback](journal-intent-readback.md), selected target coordinates and the typed v5 native POST proposal. It imports no provider transport, execution token, grant issuer or journal writer, and is outside the closed installed archive.

The join recomputes the exact PR marker, title, head and base from the selected repository and refs/SHAs. It requires byte identity with the verified plan request, exact `POST repos/{repository}/pulls`, one maximum provider write, the same operation and request digest in the journal readback, the selected target-object digest, distinct dispatch/reviewer/issuer IDs, and a committed journal generation exactly one after the selected prior generation. A red-before test found that a changed committed head passed when the result was merely a well-formed hash; the source now requires a separately selected committed generation and head. The result remains `request-consistent-but-unadmitted`, `authorized=false`, `can_dispatch=false`, `live_effects=0` and carries no body bytes or effect port.

The [controls](../../eng/tests/callable-isolated-v2-native-request-custody/test_native_request_custody.py) first failed because the module did not exist. They then exposed the committed-head false green and now cover changed request bytes, method/path/write ceiling, target and marker fields, seal event, journal fields, independent committed pin, actor separation, and zero local file/token/socket/SQLite access. Matching caller-supplied objects are not protected evidence. In particular, the grant digest and CAS acknowledgment are synthetic; no grant was issued and no provider attempt was reserved. The protected owner must separately authenticate the selected event, target/prestate, execution App scope, durable journal commit and independent committed-head pin in a new installed revision before considering a one-POST decision.

The closed archive and both effect workflows remain unchanged and disabled. [#550](https://github.com/FS-GG/FS.GG.Coordination/pull/550) native effect, [#545](https://github.com/FS-GG/FS.GG.Coordination/pull/545) disputed receipt, protected merge, Authority write and cutover remain held.
