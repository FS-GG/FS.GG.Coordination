# Closed v5 install approval reader custody

Status: source-only fake-port repair stacked on draft #784. The [Git tree result](../../eng/callable_isolated_v2_v5_git_tree_membership.py) now carries the artifact, workflow, Git object and repository-format reader identities. The [installed refusal result](../../eng/callable_isolated_v2_v5_installed_refusal.py) carries probe and no-effect audit reader identities. The [install approval join](../../eng/callable_isolated_v2_v5_install_approval.py) requires nine distinct fake principals and nine distinct public credential IDs across source, review, artifact, workflow, Git, repository format, probe, audit and approval readers. Every result remains `authorized=false`, `can_dispatch=false`, `live_effects=0`.

The [red-before test](../../eng/tests/callable-isolated-v2-v5-install-approval/test_v5_install_approval.py) showed the install approval reader could reuse the Git reader principal. Added negatives also reject reuse of artifact, workflow, repository-format, probe or audit principals, a reused Git credential ID, and collisions already present in a prior result. These are public fake reader identifiers, not protected credential material.

No reader identity here is authenticated. Protected owner admission still needs independent principals and credential custody, selected immutable integrated source, actual provider artifact and Git object observations, approved runner image/runtime and installed no-grant refusal. This draft does not enable workflows, install an artifact or admit the #550 native one-POST. #545 receipt, protected merge, Authority write and cutover remain held.
