# Disabled effect-scaffold producer workflow candidate

Status: source only, unselected and non-authorizing. `.github/workflows/callable-isolated-v2-effect-release.yml` proposes a `workflow_dispatch` producer with an exact source checkout, deterministic closed-scaffold build and one named artifact. Its job condition is `if: ${{ false }}`. Its first step exits 78 before checkout or build if the job condition is ever changed alone. The runner label and environment are deliberately unselected. No producer run or installed artifact follows from this draft.

The source-only [byte guard](../../eng/verify_callable_isolated_v2_effect_producer_workflow.py) pins SHA-256 `cb0183fe19b6b3a06e27f5112e4e65bbd8d7ceeac1c0d6693bcf260cb0be1820`. It checks exact disabled gates, source revision checkout, read-only permissions, pinned checkout/upload actions, archive build and manifest comparison, and the one artifact path. It returns `producerEnabled=false`, `authorized=false`, `canDispatch=false`, `liveEffects=0`. [Independent mutations](../../eng/tests/callable-isolated-v2-effect-producer-workflow-candidate/test_effect_producer_workflow_candidate.py) replace gates, runner, action, permission, artifact or digest and must refuse even when a caller recomputes the mutated digest. The exact candidate test denies file, environment-token, socket and SQLite use by the byte guard.

This guard reads supplied bytes only. It does not select or authenticate an integrated Git commit, runner image, protected environment, producer principal, immutable artifact or reviewer. The protected owner must obtain an independently approved exact integrated source tree and workflow blob, immutable runner/runtime pins, distinct authenticated producer and release reviewer identities, a durable approval event, and the selected artifact/readback before enabling either refusal gate. Both gates and the separate inspect-only workflow remain disabled. #545's disputed receipt and [#550's native one-attempt gate](https://github.com/FS-GG/FS.GG.Coordination/pull/550) stay held; no protected installation, credential, grant, journal/CAS, provider POST, merge, Authority write or cutover is authorized.
