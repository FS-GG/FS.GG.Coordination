# Closed native v2 effect scaffold

Status: source-only draft, 2026-09-25, stacked on the [protected owner packet](protected-owner-qualification.md). The [builder](../../eng/build_callable_isolated_v2_effect_scaffold.py) creates a deterministic local archive named `fsgg-callable-isolated-v2-effect-scaffold.pyz`. Its [manifest](effect-scaffold-manifest.json) records archive SHA-256 `c927ca5995255a79a4d8b6b637855867dad14c26bd79bfc4a31a81bb1ea628ec`, fixed ZIP members, exact source hashes and disabled workflow hash. The archive carries a closed entry, a pure effect-candidate checker and the provisional native v2 classifier bytes as inert `native-v2.source`. The native source cannot be imported from the archive as a Python module. This archive is distinct from the #555 inspect-only archive and is not a qualified execution artifact.

The [entry](../../eng/callable_isolated_v2_effect_entry.py) reserves the `execute-native-pull` command but returns status 78 and `authorized:false`, `canDispatch:false`, `liveEffects:0`. With no arguments beyond that command, the [gate](../../eng/callable_isolated_v2_effect_closed.py) returns `no-grant`; all other arguments refuse. Supplying grant bytes directly to the gate also refuses before any port call. There is no grant verifier, execution token reader, protected journal CAS, GitHub transport or dispatch implementation.

The new [execution workflow](../../.github/workflows/callable-isolated-v2-execute.yml) is a separate source-only proposal with `if: ${{ false }}`, empty permissions, unselected runner/environment/source/archive/image pins and a refusal step. It has no secret reference. The #563 inspect-only workflow remains disabled and is not repurposed. The builder pins each archive member and this workflow's exact source SHA-256; changing native source or workflow bytes refuses before archive write until reviewed pins are updated.

The [byte verifier](../../eng/verify_callable_isolated_v2_effect_scaffold.py) takes archive, workflow and canonical manifest bytes plus a caller-supplied approved manifest digest. It checks the fixed member/source pins, workflow digest, ZIP metadata and exact reconstruction of the archive bytes, then returns `authorized:false` and `canDispatch:false`. A red-before control showed that `zipfile` accepted trailing bytes when a matching rewritten manifest was supplied; exact reconstruction now refuses them. This verifier does not authenticate the caller's approved digest or establish protected release custody.

The [local controls](../../eng/tests/callable-isolated-v2-closed-effect-scaffold/test_closed_effect_scaffold.py) first failed because the archive builder did not exist. A later red-before alternate-entry probe imported the archived native module despite the default entry's refusal; the inert member closes that route. The controls now compare two clean builds and the committed manifest; mutate native or workflow bytes independently; refuse changed archive, workflow, member source, manifest or approval bytes; call the closed gate with no grant and untrusted grant bytes against denying token/CAS/POST ports; and run the archive under `python3 -I -S` from a clean temporary directory. A local canary receives zero POSTs, token and journal sentinel files remain unchanged, and an installed audit-hook probe refuses if either file is opened or a socket/SQLite connection is attempted. These are local source and installation simulations, not protected runner evidence or proof of effective App scope.

The protected owner still needs an integrated source revision, independently reviewed and read-back producer artifact, immutable runner/interpreter closure, and an explicit protected installation decision. A future **different reviewed revision** must add authenticated grant, credential, durable CAS and native transport paths, then repeat installed no-grant and one-attempt negatives with external counters before any POST window. #550's native hold, #545 disputed receipt, typed gate/index, Authority boundary and cutover remain unchanged.
