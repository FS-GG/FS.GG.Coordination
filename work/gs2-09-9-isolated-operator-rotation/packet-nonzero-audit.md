# Protected-release packet placeholder audit and owner handoff

Status: source-only GS2-09.9 qualification stacked on [#566](https://github.com/FS-GG/FS.GG.Coordination/pull/566) head `e79db1f2c89f290af07f5fe138add5991f6f22da`, 2026-09-25. No protected installation, execution credential, grant, journal/provider effect, gate/index change, receipt, merge or cutover is authorized by this packet.

## Red-before defect and exact held bytes

The [release packet verifier](../../eng/callable_isolated_v2_release_provenance.py) previously accepted nine all-zero placeholders when the synthetic packet and synthetic observer were changed together: source revision/tree/verifier SHA-256, workflow SHA-256, OCI image digest and attestation SHA-256, interpreter SHA-256, protected closure SHA-256 and standard-library tree SHA-256. The focused red-before test reported nine `Refused not raised` failures. These values cannot identify immutable installed source or runtime bytes. The repair requires nonzero lowercase hashes at each boundary, keeps the local #561 closure excluded, and rejects an all-zero OCI digest. Producer run/artifact IDs, dispatch actor, independent reviewer and run/attempt remain positive and cross-bound through the #565/#566 selection and control observations.

The repaired packet-verifier source SHA-256 is `caa51b61d3166060d31d25ec02082ba99cdb232af6a4b72ca45274f6aaec6515`. The **still disabled** inspect-only workflow has been repinned to that source; its refreshed byte SHA-256 is `c486fe746ee88536897c277fc1cbcc320813c08d4b876dacb01adb78da759e93`. Its only job remains `if: ${{ false }}` with empty permissions, an unselected runner/image and terminal refusal. The separate pure workflow verifier pins those exact new bytes. This is a new draft candidate; #563's original head `55ca33b7b45fd81889ffbccb4fbd4939f352b7d3` and template digest `1aac83c07ad0c798bd946dfecc87d16924a94455c05b566d43b44eb70aa445b9` remain historical.

## Protected owner checklist before any inspect-only installation

1. Review and publish the exact integrated source tree and workflow bytes on protected Coordination `main`; read the protected commit/tree and every #555–#566 source hash back through a distinct read-only role. No draft head or self-reported `protectedMain:true` proves this.
2. Independently review a **new runnable** inspect-only workflow revision, its exact byte digest, environment policy and distinct reviewer identity. This disabled template cannot be dispatched into an installation by input changes.
3. Select and read back the exact #555 archive `003f63ac1b0f895642a7000954607b0980f8e9f1dbb058576e66b40ed79cbfb7`, manifest `9cf485b238a458c3234adafb6f3964b7e105507a8d8cfbeb01a1d067b2545fd3`, producer run and artifact IDs. Verify immutable OCI image digest, runner host attestation, interpreter bytes/version, standard-library tree and a **new protected** closure digest. The local #561 digest is not a protected substitute.
4. Keep execution-token, journal and target credentials absent. Read back exact release run/attempt, actor, environment and approval event with a distinct active reviewer. Seal the canonical selection and packet digests independently, then compare the release, approval and installed-control observations, including no-grant/unknown-command refusal outputs, post-probe hashes, actor and producer IDs and zero effect/write counts.
5. Retain complete evidence for a second reader. Any unavailable, mutable, mismatched, zero or self-approved fact holds the inspect release. Even a complete inspect release remains `authorized:false`, `can_dispatch:false`, `live_effects:0`; [#550's separate native-effect authority](authority-amendment.md) still needs selected target, scoped App credential, protected CAS, one-use grant, one POST, complete native readback and fresh-process no-repeat.

## Independent gate-context read

The separately open Coordination [#532](https://github.com/FS-GG/FS.GG.Coordination/pull/532) head `8e53fd7e2493441276ba2f8c38015743b34b3a9b` changes the V1 provider reconciliation signature to include operation generation; its own body says the pending [#529](https://github.com/FS-GG/FS.GG.Coordination/pull/529) provider adapter must be amended before registration. [#527](https://github.com/FS-GG/FS.GG.Coordination/pull/527) head `860dc9fbd840d0391cabedd5700014f2ecf70114` adds read-only GS2-09.7 review/delivery/release capture, but its PR is blocked and several hosted jobs were cancelled. [#526](https://github.com/FS-GG/FS.GG.Coordination/pull/526) head `57b9c50888c90c329eaa87ecca305323f7698367` is a draft read-only GS2-09.7 claim/journal capture that still needs project registration after neighboring source merges. All three target `main` separately, so the labels “#532→#527→#526” do not form an installed immutable stack. Their source checks neither approve this v2 release nor supply its native operation authority. Protected owners must sequence and requalify those V1/GS2-09.7 changes on exact integrated bytes if they are needed for the shared admission route; this draft does not merge them.
