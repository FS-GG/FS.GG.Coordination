# Installed-control provenance challenge

Status: source-only qualification stacked on [#565](https://github.com/FS-GG/FS.GG.Coordination/pull/565) head `374c49175b1031b30a379220f353a6f272e75f57`, 2026-09-25. This packet is not a protected release, authority grant or GS2-09.9 receipt.

The #565 selection binds actor, producer run and artifact IDs to the release packet. Its third, installed-control observation previously named the release run/attempt and source revision but omitted the **source tree, actor and producer run/artifact IDs**. A control record with no such fields passed the pure verifier when its other synthetic values matched. A red-before test failed with `AssertionError: Refused not raised`. The repair requires all four fields and exact type/value equality to the selected values. Missing or swapped source revision/tree, actor, producer run or artifact ID now refuses. The [readback procedure](release-readback.md) names the same required evidence.

A second adversarial fixture claims #565's draft commit as a protected source revision while supplying matching local packet/observer dictionaries. It can pass shape consistency, but the result remains `authorized:false`, `can_dispatch:false`, `live_effects:0`. This is an **origin limit**, not protected provenance: no protected source reader or independent digest approval port is implemented. A hardcoded list of draft SHAs would become stale and would not prove that a commit was on protected `main` for the selected run. Before any inspect-only installation, a distinct protected reader must establish the actual `main` revision/tree and exact workflow bytes for that run, with a separately approved selection digest, immutable producer artifact and runner/image/closure evidence, and distinct environment approval. Draft #563's workflow stays disabled.

The installed controls still observe only an inspect-only zipapp. They cannot prove the protected journal CAS, selected-target App credential, one native POST, two complete provider readbacks or fresh-process no-repeat required by [#550's native authority amendment](authority-amendment.md). The GS2-09.9 typed ceiling, gate/index and acceptance receipt stay held. No credential, grant, protected install, provider/journal effect, merge or cutover occurred in this qualification.
