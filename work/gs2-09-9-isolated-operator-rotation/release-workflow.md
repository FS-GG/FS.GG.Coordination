# Held inspect-only v2 protected-release workflow proposal

Status: source-only skeleton, 2026-09-25, stacked on #562. [The proposed workflow](../../.github/workflows/callable-isolated-v2-release.yml) is not present on protected main and this draft does not run it. Its single job has `if: ${{ false }}`, an unselected self-hosted runner label, a deliberately nonexistent OCI image digest, `permissions: {}` at both workflow and job scope, no dispatch inputs, no checkout/action step and a terminal refusal. The named `callable-isolated-v2-release` environment expresses the future review boundary; its name alone is not proof that required reviewers are configured or that the actor and reviewer differ. No execution App token or secret is referenced. A source merge or manual dispatch cannot turn this skeleton into an inspect installation without a separate reviewed workflow change.

The workflow fixes the #555 archive SHA-256 `003f63ac1b0f895642a7000954607b0980f8e9f1dbb058576e66b40ed79cbfb7`, its manifest, and exact source hashes from #555–#562. This stacked proposal refreshes the packet-verifier pin while retaining the disabled job; its held template SHA-256 is `c486fe746ee88536897c277fc1cbcc320813c08d4b876dacb01adb78da759e93`. The #563 draft's earlier template digest was `1aac83c07ad0c798bd946dfecc87d16924a94455c05b566d43b44eb70aa445b9`. `eng/validate_callable_isolated_v2_release_workflow.py` checks the current digest against a separately supplied value, verifies every exact source byte, and applies an independent semantic guard to the disabled job, empty permissions, environment, unselected pins, sentinel image, refusal step and absence of token/action/effect syntax. Its result is always `authorized:false`, `can_dispatch:false`, `live_effects:0`. The supplied digest is only a local test input until a protected reviewer independently pins it; the source constant cannot approve itself.

## Protected decisions before any activation

1. Review and merge an immutable Coordination source revision containing unchanged #555–#562 bytes, this workflow and its verifier. Rebind the release packet to the protected commit/tree and workflow SHA. The current `CANDIDATE_PARENT_HEAD` is a draft head, and zero-filled expected source/workflow/packet values are intentional refusal sentinels.
2. Choose and attest a dedicated ephemeral runner host plus a read-only root/container image by OCI digest. Replace both the unselected runner label and zero image digest in a new reviewed workflow revision. GitHub's [workflow syntax](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax) allows job conditions and self-hosted labels, but the label is routing, not an immutable host identity. GitHub also warns that [environment protection does not isolate a self-hosted runner](https://docs.github.com/en/actions/reference/workflows-and-actions/deployments-and-environments); the host/image attestation and no-secret job boundary need independent proof.
3. Select a separately retained source/artifact producer run and read back exactly the #555 zipapp and manifest. Approve a new protected runtime-closure manifest from that runner; #561's local `6783d094396af466461f3dfc8e8d0e41dada34b177e1c06dd784806c79979162` is explicitly disallowed. The future job may use only separately reviewed read-only source/artifact access. It must not receive an execution App token, journal credential, target credential or cleanup token.
4. Configure the protected release environment and independently record a distinct active reviewer and exact approval event for the release run/attempt. The released workflow must verify source/workflow/image/packet pins before clean installation, then run #560's installed no-grant and unknown-command controls and #561's closure readback under `-I -S`. A second reader must verify the archive, environment approval and retained evidence. Any unknown or drifting fact holds the release.

These steps would qualify an **inspect-only** installation. The #562 packet still returns non-authorizing evidence. A later effect artifact requires separate selected-target admission, authority/issuer/credential custody, protected journal CAS, one-attempt recovery and complete native readback. The distinct one-operation reviewer approval and one-use grant in the [authority amendment](authority-amendment.md) remain necessary before any POST. The current GS2-09.9 typed ceiling, gate/index and receipt remain unchanged; this draft performs no protected installation, grant, journal/provider effect, merge or cutover.
