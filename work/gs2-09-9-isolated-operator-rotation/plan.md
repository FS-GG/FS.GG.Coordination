# GS2-09.9 isolated operator rotation candidate

Status: provisional source preparation, 2026-09-25. This packet does not authorize an effect, merge, unit receipt, or GS2-09.9 acceptance.

## Defect and preserved evidence

The previously sealed `eng/callable-cli-isolated-operation.py` has three demonstrated gaps: ambiguous pull request creation can accept a sole wrong-head or wrong-base candidate; ambiguous protection setup can accept force pushes; and a URL exception chain can retain sensitive response text. [PR #547](https://github.com/FS-GG/FS.GG.Coordination/pull/547) retains the offline red-before characterization. The old Q3 validator's 28 controls do not cover these cases. PR #545's earlier Q3/Q6 exit-zero evidence is therefore disputed even if its six required hosted checks pass. Auto-merge is disabled and #545 remains unmerged. The old source, `/4` contract and proposal, old validator, native acceptance archive, and their digests remain historical bytes. A passing historical native operation is one observed synthetic subject, not proof that every ambiguous result is safe.

The [versioned source PR #549](https://github.com/FS-GG/FS.GG.Coordination/pull/549) and its [stacked loopback transport PR #551](https://github.com/FS-GG/FS.GG.Coordination/pull/551) provide an injected runtime reader and a localhost-only HTTP transport with 22 focused controls. The CLI can exercise a simulated POST or PUT against a local transcript and SQLite attempt fence. The separate qualification harness stages exact source bytes and executes the same runtime against a real local HTTP server. Neither path exposes a GitHub credential or live provider target. This packet binds those exact source and control files by SHA-256 and labels the old preflight as `historical-observation-only`. The preflight's 2026-09-18 observations do not assert current provider authority.

## Proposed source bindings

| Role | Path | SHA-256 or binding |
| --- | --- | --- |
| Versioned source | `eng/callable-cli-isolated-operation-v2.py` | Stacked #551 head `c3dd33d7aac1c22e953b51406bba1bb21b5e38c2`; SHA-256 `23435c0c77893100d98c5e387ef3b0d3067422f2cb1368bcd7883e3fb212232c` |
| Focused controls | `eng/tests/fsc07-isolated-operation/test_versioned_operator_readback.py` | SHA-256 `9d6c4c3a74ac062c5186eb4023030a2f80cb11bce64262b1f4a48fe8f627c64f` |
| Independent loopback harness | `eng/qualify-callable-isolated-v2-loopback.py` | Stages exact source/contract bytes; real 127.0.0.1 HTTP, lost responses, explicit 302/401 refusal, 500 readback, wrong poststate, fresh-process SQLite no-repeat; bound by `loopbackControls` |
| Historical preflight | `evidence/github-substrate-v2/gs2-09-9/isolated-operation-preflight.json` | Raw SHA-256 `ca9ee64633866425ced8d4578964df70eb3dc4cac2fd420f2d2963d5222e7de8`; self-digest `46c9af2263a8f686c59e90aacb79cc5466c2e1d3c6422221f86b1373fe5813eb` |
| Provisional contract | `eng/callable-cli-isolated-operation-v2-contract.json` | `/5`, `prepared-not-authorized`, exact source/control/historical bindings |
| Provisional proposal | `eng/callable-cli-isolated-operation-v2-proposal.json` | `/5`, exact contract self-digest and source binding, same historical observation |
| Offline validator | `eng/validate-github-callable-isolated-operation-v2.fsx` | Exact old-byte immutability, v5 bindings, zero-effect inspect, 22 focused controls and the independent loopback harness |

The v5 operator's `inspect` result must remain `authorized:false`, `historicalObservationOnly:true`, `liveEffects:0`, and `refused-no-compatible-admitted-target`. The `exercise-offline` command uses an injected transcript and local attempt fence; its results must retain `simulationOnly:true` and `liveEffects:0`. Both `--phase contract` and `--phase recovery` in the provisional validator check the exact source, all 22 focused tests and the independent loopback cases, then require the named Q3 or Q6 controls for that phase. A green result proves this bounded source packet only.

## Proposed gate rotation and remaining closure

The eventual exact candidate must register new Q3 and Q6 gate IDs and command digests in `eng/github-substrate-v2-gates.json`, then update the GS2-09.9 `gateCommands`, `gateContracts`, exit text, and `contractSha256` in `eng/github-substrate-v2-units.json`. The historical protected acceptance Q6 validator remains a separate gate. The old Q3 source validator must not be counted as proof of the repaired ambiguity semantics. The gate catalog and typed index are intentionally untouched in this provisional PR because the versioned source is still draft and the full exit gate is unresolved.

The proposed Q3 gate ID is `github-callable-isolated-operation-v2-contract`, running `dotnet fsi eng/validate-github-callable-isolated-operation-v2.fsx -- --root . --phase contract`. The proposed Q6 gate ID is `github-callable-isolated-operation-v2-recovery-contract`, running the same script with `--phase recovery`. Their command hashes, the final v5 source/control digests, the catalog digest, and the GS2-09.9 contract digest must be computed only from the final integrated candidate. `RoadmapWorkArchitectureTests.fs` literal expectations must follow the accepted catalog/index bytes without weakening its stale-index negatives. A prospective accepted receipt and `evidence/index.json` row are a later, separate protected acceptance change.

The current catalog has 75 commands. Adding both proposed IDs would make 77; replacing the old isolated Q3 command instead would make 76. The final typed exit decision must choose and document which historical command remains registered. Each `commandSha256` is SHA-256 of the executable and argument strings joined with NUL bytes, as implemented in `RoadmapWorkArchitectureTests.fs`. No command count or digest is changed by this packet.

The shared rotation touch set also includes the existing literal GS2-09.9 contract digest in `eng/validate-github-callable-ordinary-delivery.fsx`; otherwise both ordinary Q3 and Q6 refuse the newly registered unit. This literal must be changed to the recomputed final contract digest and the ordinary gates rerun on the same candidate. Keep the old proposal and native acceptance files as immutable historical inputs to those ordinary gates.

| Gate | Controlled case | Required result |
| --- | --- | --- |
| Q3 | One PR POST returns a complete response or loses its response; two complete native reads bind exact repository ID, source ref/head, base ref/head, PR number/node, and operation identity | Accept only the unique exact candidate; one POST attempt |
| Q3 | A sole PR after response loss has a wrong head, base, repository, or operation identity | Unknown or refusal; no second POST and no accepted PR |
| Q3 | PR census is incomplete, duplicated, or changes between complete reads | Unknown or refusal; no accepted PR |
| Q3 | Listed PR node/repository differs from its detail, terminal Link contradicts an earlier advertised last page, or JSON repeats an identity/policy member | Unknown or refusal; no accepted PR or protection |
| Q3 | A singleton read supplies an unexpected Link header, or the terminal repository/ref/branch identity changes after the final PR detail or policy response | Unknown or refusal; no complete poststate claim |
| Q3 | One protection PUT returns a complete response or loses its response; two complete native reads bind the protected branch and full required policy | Accept only the exact no-force-push, no-deletion policy; one PUT attempt |
| Q3 | Protection readback permits force push or deletion, has missing policy fields, targets another branch, or drifts between reads | Unknown or refusal; no accepted protection |
| Q6 | Response loss occurs before or after a persisted attempt record, and the process restarts | Reconcile by exact native readback; never repeat an uncertain write |
| Q6 | The same planned isolated write is replayed in a fresh process against the durable local attempt journal | Unknown with zero new write attempts; the separate ordinary CLI Q6 gate still proves `AdvanceAlreadySettled` replay |
| Q6 | The provider returns explicit 302 or 401 despite an exact scripted poststate | Unknown with the explicit-refusal reason; no redirect is followed and no Applied result is attributed |
| Q3/Q6 | Transport, HTTP, or URL exception text contains a secret sentinel | Public refusal or unknown result contains no sentinel, including its exception chain and traceback |
| Q3/Q6 | Contract, proposal, source/control bytes, old preflight, or native readback drift | Refusal before effect; retained historical evidence remains unchanged |

Before registration or receipt, the corrected versioned operator needs reviewed controlled end-to-end evidence for one durable attempt, complete native poststate, loss reconciliation, and replay. The #551 controlled runtime and independent harness exercise these code paths from a staged exact source copy over real loopback HTTP. They cover wrong PR head, force pushes, incomplete or drifting native reads, explicit 302/401 refusal, 500 and lost-response readback, a sensitive-body sentinel, and fresh-process no-repeat. The harness reports `artifactKind:staged-source-copy`, `stagedExactArtifact:true`, `loopbackHttp:true`, `authorized:false`, and `liveEffects:0`. Independent review concluded that this does not establish the installed provider, credential and capability path, or an accepted native operation. The ordinary Q3/Q6 gates must continue to exercise the installed CLI production boundary on the same final candidate. The typed exit gate must not be narrowed to accept the staged copy.

## Smallest prospective protected v2 native operation

The native v1 archive is a preserved historical observation of installed CLI 0.1.1 and the old operator. It cannot qualify the corrected v2 source. The current [`.github` executor](https://github.com/FS-GG/.github/blob/main/.github/workflows/callable-isolated-operation-execute.yml) checks out fixed v1 Coordination revision `bcb8453c3b92a9ea6097e66e326bd1ec3350b667` and invokes the v1 operator; its execution token also has permissions beyond the proposed one-POST role. The new v2 CLI exposes `inspect` and `exercise-offline` only. Thus no installed v2 GitHub effect path exists today. The next source change needs a separately reviewed live command and a protected executor pinned to its immutable source/artifact. Neither the existing workflow nor this draft may be dispatched for v2 by changing input strings.

The current GS2-09.9 typed unit in `eng/github-substrate-v2-units.json` permits historical native revalidation only and expressly excludes live provider mutation and new external acceptance. The checked roadmap's completed GS2-09.9 line records the historical v1 subject; it does not silently enlarge that ceiling for a corrected v2 run. As with the earlier separate V2-CALL-01.4b operation authority, a distinct protected owner amendment must first name this one synthetic v2 POST, its actor and target ceiling, credential custody, native proof, independent reviewer and relationship to the historical receipt. That narrow operation authority permits only the named run; it does not register a Q3/Q6 gate or accept GS2-09.9. After native evidence exists, a later protected candidate can rotate the typed unit, gate catalog and exact roadmap pin without treating the grant as a unit receipt. This plan and draft #550 create neither authority.

The minimum new native action is **one synthetic pull-request POST** in a fresh, disposable, explicitly admitted target. It does not reuse deleted repository `1376575900`, the GS2-09.7 sandbox, a production repository, or a live Project. Source and base branches must already exist under a separately admitted setup action with its own receipt; target creation, branch writes, and journal initialization are not counted as the single provider POST. Protection PUT remains a forbidden effect in this one-write window; its corrected readback is exercised by the loopback controls and must be qualified in a later separately bounded native window if the final typed acceptance contract requires native protection mutation. This one-POST plan cannot itself prove a protected PUT occurred.

| Stage | Required exact input and evidence | Refusal or stop boundary |
| --- | --- | --- |
| Protected source install | Merge and read back the corrected v2 operator, focused controls, live transport entry point, and exact source/archive digest. A protected `.github` plan/authorize/execute revision pins those bytes, the reviewed `/5` contract and the distinct one-operation authority. Source delivery must use the then-admitted common V1 route; an old direct merge supplies no admission. | Draft source, unmerged workflow, mismatched artifact, absent operation authority, or unresolved credential custody prevents plan issuance. |
| Target selection | Independently read a fresh synthetic-only repository's numeric and node ID, exact owner/name, visibility, installation selection, source/base branch refs and SHAs, existing PR census, protection, and cleanup capability. Bind them in one sealed plan. A target is **not selected yet**. | Missing entitlement, foreign repository, branch or policy drift, incomplete pages, existing operation marker, or a target requiring billing/visibility change refuses before any effect. |
| Credential custody and authority | Protected-main workflow and exact environment require an independent reviewer distinct from the dispatch actor. The `.github` authority observer reads run, environment and artifacts; the membership observer checks reviewer identity; a target observer reads installation, repository, refs, PR census and policy; a separately authorized setup token owns any earlier target/branch writes; the selected-repository execution App installation token has `pull_requests:write` and the minimum required read scopes; a distinct cleanup token is unavailable to execution. None of these tokens is printed or retained. A short-lived protected grant binds run ID/attempt, workflow revision and digest, reviewer ID, reviewed source/contract/artifact and amended unit, plan seal, target numeric/node ID, operation ID, allowed `POST /repos/{owner}/{repo}/pulls`, expiry and no cleanup permission. The installed operator independently verifies the protected run/environment/reviewer, App installation and effective token repository/permission selection immediately before dispatch. | Dispatch inputs are lookup hints only. Self-review, expired or replayed grant, broad/unknown token scope, changed workflow, missing approval, or secret in retained output refuses. An actor's repository `admin` flag does not stand in for a scoped App credential. |
| Durable intent and one attempt | Commit the operation ID, exact request-body digest, target/source/base identities, allowed effect and attempt state to a protected compare-and-swap journal before POST; read back its committed generation with a separately authorized journal credential. The v2 operator's current local SQLite fence is controlled-test evidence, not this cross-run journal. Only after the journal readback and fresh authority checks may the selected-repository execution token issue exactly one canonical POST. Persist that dispatch may have occurred before interpreting any HTTP response. A fresh process reads the same journal before deciding whether a write is possible. | No durable compare-and-swap/readback means no POST. After an uncertain dispatch, never issue a second POST for the same operation ID. Journal writes have their own authority and receipt and are outside the one provider POST count. |
| Native closure | Retain sanitized request/response hashes and two complete independent GitHub repository/ref/PR-page/detail readbacks after dispatch, including terminal pagination, PR number/node, head/base repo IDs and refs/SHAs, operation marker, protection and terminal branch identities. Compare both with the complete prestate and sealed plan. A 2xx response still needs exact native readback. Lost response or 5xx may classify only from complete stable native poststate; explicit 3xx/4xx stays Unknown. Bind the native run, artifact IDs/digests and independent observer readback to a sealed receipt. | Wrong head/base, foreign list row, contradictory pagination, changed terminal ref, unknown response with incomplete reads, or missing artifact yields Unknown and stops; it never creates another PR. |
| Replay and cleanup | From a new process and the retained CAS generation, prove zero repeat POST with the same installed artifact and operation ID. Record a separate protected cleanup intent and decision only after the native receipt has been independently verified. Close/delete the synthetic target solely through a distinct expiring cleanup grant with exact target/PR readback. | No automatic compensation after Unknown. Cleanup 404 is a post-cleanup fact only; it cannot replace the pre-cleanup native PR receipt. No receipt, Q3/Q6 registration, or unit acceptance follows a partial run. |

The exact authorization point is the protected environment approval and issuance of the one-use execution grant **after** the separate one-operation authority and source/workflow are installed, the setup receipt and fresh complete target prestate are sealed, the credential/installation scopes are read back, and the durable journal generation is committed and verified; it is **before** the one POST. The independent reviewer must see that exact packet and approve the named run/attempt. A queued workflow, dispatch input, source merge, GS2-09.7 Q4 mint proof, or broad repository permission does not authorize the effect. Revalidate expiry, identities, scopes, prestate and journal generation at dispatch; any drift cancels the grant without writing.

Prospective qualification requires the installed artifact hash, protected plan and grant artifacts, capability/permission readback, CAS intent/attempt generations, native pre/post provider transcripts, one-write count, fresh-process replay, independent receipt verification, cleanup receipt, and retained failure artifacts for every refused branch. The protected gate must check these against the exact candidate source, catalog, amended typed contract and roadmap pin. A controlled source test may remain alongside this evidence, but cannot substitute for it. The operator currently lacks the live entry point, protected CAS integration, and sealed v2 plan/grant workflow; those are concrete implementation blockers. No authorization request or dispatch is appropriate until those bytes, target and reviewer route are reviewable.

The [effect-adapter proposal](effect-adapter.md) names the exact installed CLI gap, closed entry, credential and journal ports, and required negative controls without adding an effect route to this draft.

Once those clauses are implemented and reviewed, qualify one fresh immutable replacement candidate at the exact live roadmap pin with complete Q3/Q6 gates, required hosted checks, independent native archive/readiness/handoff revalidation, and protected merge readback. That candidate must carry #545's narrow roadmap pin and the reviewed v5 rotation together. PR #545 remains unmerged with auto-merge disabled, then closes as superseded after the replacement's protected acceptance. Append a prospective `accepted/GS2-09.9.json` receipt and evidence-index row using the replacement's actual protected merge and acceptance time. Preserve #531's source merge, #545's disputed candidate, and the historical isolated run as distinct facts.
