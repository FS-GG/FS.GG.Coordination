# Inspect-only v2 runtime closure candidate

Status: local source and test evidence, 2026-09-25. This draft stacks on #560. It adds an import-only closure capture/verifier and [one local candidate manifest](runtime-closure-local.json); it installs no protected workflow or credential and does not issue a grant, write a journal, contact a provider or enable a dispatch route. GS2-09.9's typed ceiling, gate/index and receipt remain held.

The verifier first runs #560's exact archive/source/interpreter check. It then uses the pinned interpreter with `-I -S`, an empty working directory and a sanitized environment to exercise the exact inspect archive's closed `--help` entry. Its instrumented process records the canonical standard library root and all file-backed mappings visible in `/proc/self/maps`. It hashes every regular file and directory name in the standard library tree except `site-packages` and `dist-packages`, which `-S` leaves outside the admitted import path. It hashes each mapped file separately, including the interpreter, loader, shared libraries, native Python extensions and loader cache. Paths, sizes and SHA-256 values are canonicalized into `fsgg.coordination.callable-isolated-v2-runtime-closure/1`. Verification requires an independently supplied manifest SHA-256, rejects duplicate or noncanonical JSON, independently recomputes the closure and compares exact bytes. Results remain `authorized:false`, `can_dispatch:false`, `live_effects:0`.

On this **unprotected local runner only**, the resolved binary was `/usr/bin/python3.14` (CPython 3.14.7, SHA-256 `d78f9cf7178ecff09963551399855543c297f37ac207e626228bfe43cb26a70c`). The observed standard library root `/usr/lib/python3.14` had 112 included directories, 2,765 files, 68,793,106 bytes and tree SHA-256 `eb900b66a059f32f9efefce9f4fa678233a02e2a042af074713f48cb9da68ed1`. Seventeen mapped files included the dynamic loader, libc, libpython, libcrypto, libz, libzstd, brotli libraries, five native Python extensions, the binary and `/etc/ld.so.cache`; the [manifest](runtime-closure-local.json) gives their exact paths and hashes. Its 3,108 canonical bytes have SHA-256 `6783d094396af466461f3dfc8e8d0e41dada34b177e1c06dd784806c79979162`. These coordinates describe this machine and are **not** a protected release pin.

Independent local controls mutate the captured manifest even with a recomputed caller digest, change standard library file contents, add and remove files or an empty directory, replace a mapped-library fixture, introduce symlinks, and alter the archive after the probe. Each mutation changes the seal or refuses. The positive probe receives no inherited token or journal path; it leaves a native-journal sentinel untouched. The inherited #555 clean-install test supplies the local HTTP canary and verifies zero POSTs. The probe observes one closed code path and mapped files, not every file the OS might read or every future execution branch. In particular, `/proc/self/maps` does not prove all nonmapped loader/configuration reads. A protected immutable runner image and read-only filesystem pin are required to close that gap.

Before a protected install, the release owner must select and independently seal the Coordination source revision, #555 archive/manifest bytes, #559 authority source, #560 verifier source, this closure verifier source, `.github` workflow revision/hash, runner or container image digest, interpreter binary/path/version, standard library tree and mapped-file closure, loader configuration, and the exact `-I -S` command. The protected workflow must regenerate and read back its own manifest with no execution credential present, compare it to an independently approved digest, run the installed no-grant/unknown-command controls and retain the evidence. The local manifest must not be copied as that approval. Any runtime or source drift requires a new release pin and review.

The inspect archive still contains no trusted authority, journal, target or provider adapter. A later effect artifact must qualify those independent roles and a durable one-attempt native readback path. Only after that qualification and target/setup packet can the distinct reviewer approve the exact protected run/attempt and a one-use grant under the [authority amendment](authority-amendment.md). No such authorization is requested by this draft.
