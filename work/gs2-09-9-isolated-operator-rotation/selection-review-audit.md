# Selection-review closure for a future inspect-only release

Status: source-only GS2-09.9 qualification stacked on [#568](https://github.com/FS-GG/FS.GG.Coordination/pull/568) head `d3f04937997db6c938e926f4e9975db19cc94144`, 2026-09-25. This is neither a protected reviewer event nor an authority grant.

The #564–#568 pure readback checker accepted a caller-created canonical selection and its caller-computed SHA-256 when three synthetic release, environment-approval and installed-control observers agreed. A red-before test required refusal without a **separate selection-review observation** and failed with `Refused not raised`. The source now requires a fourth distinct read-only observer. Its exact `fsgg.coordination.callable-isolated-v2-selection-review/1` record binds the canonical selection bytes/digest, active reviewer ID and separate event ID, and a review time between environment approval and expiry. The selection bytes bind protected source revision/tree, workflow digest, #555 archive and producer run/artifact IDs, immutable OCI runner image and attestation, interpreter/closure/stdlib hashes, packet digest, release run/attempt, actor and reviewer. Missing, self-reviewed, stale, future, swapped or incomplete synthetic review data refuses.

The new `SelectionReviewObserver` is only a source `Protocol`; no protected implementation, event API, credential or trust root is installed. Distinct Python objects and matching fixture dictionaries cannot establish distinct protected custody. All successful local results remain `authorized:false`, `can_dispatch:false`, `live_effects:0`. The [readback procedure](release-readback.md) now names four protected roles. The first owner decision is still independent review of immutable integrated source and a new runnable **inspect-only** workflow with an attested runner and separately approved artifact/packet pins. The #563 workflow remains disabled. One native POST needs the separate [#550 operation amendment](authority-amendment.md), target/credential/CAS/grant and complete native readback; gate/index and receipt remain held.
