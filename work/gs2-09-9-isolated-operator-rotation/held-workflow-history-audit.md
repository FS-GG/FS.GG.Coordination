# Disabled workflow history and protected owner decision

Status: source-only GS2-09.9 challenge stacked on [#567](https://github.com/FS-GG/FS.GG.Coordination/pull/567) head `12f70ed6c932959b93058817f4284231c56100b1`, 2026-09-25. This draft does not enable or dispatch a workflow, install a protected artifact, read a credential, issue a grant, write a journal, call a provider, rotate gate/index, merge, write a receipt or cut over.

The #564 selection parser refused the **current** disabled workflow digest dynamically. After #567 refreshed that disabled template to pin repaired packet-verifier source, #563's earlier digest `1aac83c07ad0c798bd946dfecc87d16924a94455c05b566d43b44eb70aa445b9` could pass as a selected workflow when paired with matching synthetic packet and observer data. The red-before test failed with `Refused not raised`. The parser now refuses both known disabled template digests, retaining #567's refreshed `c486fe746ee88536897c277fc1cbcc320813c08d4b876dacb01adb78da759e93` as an explicit historical value even after a future re-pin. The workflow itself remains `if: ${{ false }}` with empty permissions and an unselected runner/image.

The exact #555 archive and manifest SHA-256 remain `003f63ac1b0f895642a7000954607b0980f8e9f1dbb058576e66b40ed79cbfb7` and `9cf485b238a458c3234adafb6f3964b7e105507a8d8cfbeb01a1d067b2545fd3`. The #567 packet verifier rejects zero placeholders; #565/#566 bind source revision/tree, actor, producer run/artifact IDs and installed control hashes. A synthetic OCI image digest, source-tree claim or active-reviewer dictionary can still pass **local consistency** if all caller-supplied objects agree. No protected reader, independent digest approval, runner attestation or real approval observer exists in these drafts, so that result always remains `authorized:false`, `can_dispatch:false`, `live_effects:0`.

The first protected owner decision is to review an exact integrated Coordination source commit/tree and a **new runnable inspect-only workflow proposal**, with immutable runner host/image and source/artifact pins, distinct release reviewer and read-only observation custody. Only after those bytes and roles are independently approved should a protected install run be scheduled and read back. That review does not authorize a native POST. The separate [#550 operation amendment](authority-amendment.md) still requires selected disposable target, scoped execution App credential, protected CAS and one-use grant, one-attempt recovery and complete native readback. The GS2-09.9 typed ceiling and gate/index/receipt remain held.
