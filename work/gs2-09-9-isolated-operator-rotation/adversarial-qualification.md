# Adversarial qualification and protected hold: isolated v2

Status: source-only challenge, 2026-09-25. This packet is non-authoritative. It changes no workflow, protected installation, credential, grant, journal/provider state, gate/index, receipt or cutover. Coordination protected `main` was `b8225f6b63f764f58ec20735d3cf849b37c57276` at the read checkpoint; the drafts below are separate heads, not installed protected source.

| Draft | Exact head | Role |
| --- | --- | --- |
| [#550](https://github.com/FS-GG/FS.GG.Coordination/pull/550) | `19323acc60f6f1314b9c92030c203bf2789e21ad` | Proposed native one-POST authority amendment; no selected target or grant. |
| [#554](https://github.com/FS-GG/FS.GG.Coordination/pull/554) | `6e261131dff2b4d51a00545836177557562a035d` | Closed one-use grant parser. |
| [#555](https://github.com/FS-GG/FS.GG.Coordination/pull/555) | `70cb2fa6e8d7fa49d716e4febc2c07d88160c058` | Inspect-only deterministic zipapp. |
| [#559](https://github.com/FS-GG/FS.GG.Coordination/pull/559) | `256a8ff7e46e20467256a63f396ebe4b8ef830cf` | Closed authority/issuer/journal interfaces. |
| [#560](https://github.com/FS-GG/FS.GG.Coordination/pull/560) | `e79417a51bd9082a87d1450fe532df5eb338b874` | Exact archive/interpreter clean-install check. |
| [#561](https://github.com/FS-GG/FS.GG.Coordination/pull/561) | `83cade703a2ee78ab4f3e4862f29fbc18bc967d6` | Local runtime closure candidate. |
| [#562](https://github.com/FS-GG/FS.GG.Coordination/pull/562) | `5ffa942bb089ba3f376c13432730c9976aa72895` | Pure protected release packet contract. |
| [#563](https://github.com/FS-GG/FS.GG.Coordination/pull/563) | `55ca33b7b45fd81889ffbccb4fbd4939f352b7d3` | Disabled inspect-only workflow skeleton. |
| [#564](https://github.com/FS-GG/FS.GG.Coordination/pull/564) | `eaff5773435a9c6b10e443103573bc09531fbbdb` | Selection/readback contract before this challenge. |

## Finding and focused repair

The #564 selection explicitly pinned archive and manifest hashes, image and runtime hashes, packet digest, run ID/attempt and reviewer, but omitted **dispatch actor ID, producer run ID and artifact ID**. A synthetic packet could change one of those omitted values, be rehashed, and be paired with matching synthetic observer data while the pure consistency check returned non-authorizing evidence. That did not create a dispatch path, but it left the independent selection unable to name those coordinates directly. This stacked source change adds the three exact positive IDs to the canonical selection schema and compares them with the validated packet. It also rejects a selected actor equal to the selected reviewer. The packet SHA still binds all bytes; explicit IDs make swaps visible even if a candidate recomputes that SHA.

The focused adversarial tests now reseal altered synthetic packet bytes while retaining the originally selected actor, run/attempt, producer run, artifact and OCI image. Each swap refuses as `readback-selected-binding`. A self-approved release, missing approval, wrong protected observation, incomplete installed control record, mutable or zero image and held #563 workflow digest also refuse. The archive remains SHA-256 `003f63ac1b0f895642a7000954607b0980f8e9f1dbb058576e66b40ed79cbfb7`; its manifest remains `9cf485b238a458c3234adafb6f3964b7e105507a8d8cfbeb01a1d067b2545fd3`. The #563 disabled workflow SHA-256 `1aac83c07ad0c798bd946dfecc87d16924a94455c05b566d43b44eb70aa445b9` is refused as a selected runnable workflow. No actual image or protected runtime digest is selected.

For the native hold, an ambiguous CAS acknowledgment (`response-unknown`) refuses even if a synthetic journal readback looks complete; a partial readback also refuses. Repeating the pure reservation check returns `authorized:false`, `can_dispatch:false`, `live_effects:0` twice, and a replay observation with `used:true` refuses. These tests do **not** prove atomicity or no repeat across real processes: there is no protected v2 journal CAS adapter or installed provider command. An uncertain attempt marker must remain a stop condition until independent durable readback and native reconciliation; a second POST cannot be inferred from a successful pure check.

## Exact protected decisions still needed

For an inspect-only installation, a distinct release owner and reviewer must first approve a protected Coordination source commit/tree containing the exact reviewed #555–#564 chain and this repair; a new runnable workflow revision and its byte digest; exact producer run/artifact IDs and #555 archive/manifest readback; an ephemeral runner host, immutable OCI image digest and attestation; interpreter, standard-library and protected closure digests; and a sealed packet digest. A named GitHub environment alone is not reviewer proof. The selected run/attempt, actor, environment and approval event must be read back from separate protected roles, with the reviewer distinct from the actor. The installed no-grant and unknown-command refusals, post-probe hashes, absent execution token and zero provider/journal/write counts require complete independent readback. #563 remains disabled and local tests cannot supply these protected coordinates.

For **one native PR POST**, [#550's amendment](authority-amendment.md) still needs a separately selected fresh synthetic repository with numeric/node ID, source/base refs and complete prestate; a protected v2 effect artifact and `.github` executor pinned to immutable source/workflow; an execution App installation selected only to that repository with exact minimal scopes; independent authority/target/membership readers; distinct setup, journal and cleanup custody; a protected CAS intent and `attempt-may-have-started` generation with readback; exact run/attempt and independent operation reviewer; and a short-lived one-use grant. The authorization point is that reviewer approval and grant issuance after target, credential and journal facts are sealed, immediately before at most one POST. Lost response or 5xx requires two complete native reads and fresh-process no-repeat; explicit 3xx/4xx stays Unknown. None of those live coordinates is supplied by the inspect-only release packet. The current typed GS2-09.9 ceiling excludes new provider mutation, and [#545](https://github.com/FS-GG/FS.GG.Coordination/pull/545) remains open with its disputed historical evidence. Gate/index and receipt stay held.
