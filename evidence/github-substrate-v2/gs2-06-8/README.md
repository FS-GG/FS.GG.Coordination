# GS2-06.8 fleet dry-plan evidence

This directory retains two complete, read-only GitHub REST observations of the
accepted ten-repository roster. Each observation covers the twelve settings
surfaces named by the qualification contract and records the exact repository
identity, default branch, observation instant, terminal pagination proof,
required read permission, response status, payload digest, and explicit
disposition.

`desired-state.json` deliberately requests snapshot preservation. Consequently
the deterministic plan contains no operation: owned repositories are no-op or
retain an explicit unsupported/indeterminate disposition, while
`EHotwagner/S.I.R.` is external-observe-only. The focused tests exercise
would-update operation identity, least-permission metadata, every exceptional
disposition, canonical replay, review, and relevant drift. The Q5 validator
recompiles both observations, performs an independent typed review, and proves
the second authoritative read did not invalidate the plan. No credential,
response body, apply capability, or production mutation path is retained.

