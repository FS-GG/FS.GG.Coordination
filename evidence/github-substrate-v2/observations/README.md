# External observations

Compact observations captured from named external systems. Records include
provenance and observation time; large source captures use an immutable store.
An observation may bind a strict domain receipt stored at its governed source
boundary by recording that receipt's exact SHA-256.
