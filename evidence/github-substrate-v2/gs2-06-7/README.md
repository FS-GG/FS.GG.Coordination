# GS2-06.7 workflow-selection qualification

`corpus.json` is the complete versioned qualification input for the pure Q3 selector and distinct Q7 fleet-safety projection. `independent-expectations.json` is authored separately and binds one independently named mutation fixture to every required control. Neither validator publishes workflows or mutates fleet state.
