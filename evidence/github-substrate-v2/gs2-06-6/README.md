# GS2-06.6 release-hardening evidence

`corpus.json` is the complete retained release-plan input bound to the accepted GS2-06.5 receipt and exact accepted roadmap bytes. `independent-expectations.json` is the independently authored control and result oracle: it binds one separately named negative fixture to every required control and retains fail-closed unknown-property cases for the corpus, feed, recovery, expectation, and independent-case object boundaries. The exact Q3 gate parses both, rejects any property outside those sealed shapes, and executes generated and independent attacker paths without any production GitHub or release mutation surface.
